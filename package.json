{
  "name": "kyoto-ts",
  "private": true,
  "description": "TypeScript workspace for Quarto Rust monorepo",
  "workspaces": [
    "ts-packages/*",
    "hub-client"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "dependencies": {
    "@types/katex": "^0.16.8",
    "@types/morphdom": "^2.3.0",
    "html2canvas": "^1.4.1",
    "katex": "^0.16.28",
    "morphdom": "^2.7.8"
  }
}
