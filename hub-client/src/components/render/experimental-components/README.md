these are simple custom render components that I copy & pasted from `.tsx` files I had in Quartohub.

I saved them with a `.txt` extension so they don't mess with the build.

Here's how an example custom render component is used:
```
format: q2-debug
render-components:
  my_component.tsx
```