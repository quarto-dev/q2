# Plan: Book-aware preview for `q2 preview` / hub-client (book-projects P8)

**Date:** 2026-09-22 (revised after an implementor-standpoint technical review the same day — see the epic doc's revision history for what changed and why)
**Epic:** [`2026-09-21-book-projects-epic.md`](2026-09-21-book-projects-epic.md)
**Design (authoritative):** [`../designs/book-projects-architecture.md`](../designs/book-projects-architecture.md) §12
**Q1 reference:** none — Q1 has no live, cross-file-aware preview at all (its editor preview is the same single-document-at-a-time model Q2's currently has); this phase has no port target, only Q2's own crossref-design plan's unbuilt `StaticProjectAnalyzer` sketch (`claude-notes/plans/2026-04-15-crossref-design.md`, Phase 4).

## Overview

P0–P7 gave `run_with_book_support()` (design doc §3 — a concrete method on `ProjectPipeline<'a, RenderToFileRenderer<'a>>`, not a `ProjectType` trait override, resolved by a seventeenth-pass research round) real chapter numbering, single-file merge, and project-wide crossref/bibliography resolution — but none of it reaches `q2 preview`/hub-client, which renders one active document through an entirely different, per-document code path (`RenderToPreviewAstRenderer`) that never calls `run_with_book_support()`. Today a previewed chapter renders exactly as if it were a lone, non-book document: no chapter number, no title decoration, and any cross-chapter `@ref` shows as the existing generic unresolved-placeholder (`?fig-ch1-plot?`), not a working link.

This phase closes the numbering/title-decoration gap (cheap — reuses P0/P4's existing machinery) and builds the one genuinely new piece the original epic explicitly deferred: a pre-engine `StaticProjectAnalyzer` that gives preview an approximate, cross-chapter-aware crossref resolution, feeding **P5's own `CrossChapterCrossrefResolveTransform` unmodified** — not a new resolution mechanism — plus the small wire-contract addition needed for the resulting link to navigate to the right chapter in hub-client. It deliberately does **not** attempt cross-chapter bibliography preview or single-file-merge preview — see Decisions.

## Decisions

- **Chapter-number seed + title decoration reuse P0/P4's mechanism as-is, applied to every swept sibling, not just the previewed document.** Build the full `HashMap<PathBuf, ChapterSeed>` — reusing **P4's** existing chapter-number/appendix-flag computation (corrected by a ripple-check pass: this was previously misattributed here to "P1's existing... logic"; P1 only builds the `BookRenderItem` list itself, P4 is the phase that turns it into the `HashMap<PathBuf, ChapterSeed>`, per P4's own Decisions) — once per preview call that touches a book project. Apply the matching entry both to the previewed document's own render *and* to every sibling `StaticProjectAnalyzer` sweeps below — a sibling's local section counter can't produce a correctly chapter-scoped approximate number without its own chapter-number seed.
- **Found during implementation (2026-09-25), resolved with the user: local (same-chapter) crossref numbers need `resolved_number` composed at Crossref-phase resolution time, not just cross-chapter ones.** `format_crossref_number(order, chapter_seed)` — the composition that turns a document's own `ChapterSeed` plus a raw per-type counter into a display string like `"2.1"` — is only invoked today by `CrossrefRenderTransform`'s `render_resolved_ref` (Finalization phase). That transform is on preview's exclusion list (`Q2_PREVIEW_TRANSFORM_EXCLUDED`), since preview needs the still-structured `CrossrefResolvedRef` node, not flattened HTML. So wiring a `ChapterSeed` onto the previewed document alone (the bullet above) is not sufficient: a same-chapter figure/table/equation reference would still preview with a flat, wrong number (`"Figure 1"` instead of `"Figure 2.1"`), failing this plan's own checklist test that previewing chapter 2 shows `"Figure 2.1"`. **Sec-type refs are unaffected** — their numbering comes from the section-stack counter, which `CrossrefIndexTransform` already offsets by the seed (P0's decision), so the raw `order.section` is correct pre-Finalization with no extra composition needed. **Fix**: `crossref_resolve.rs`'s local resolution (`CrossrefResolveTransform`, Crossref phase — always present in preview) now also composes `resolved_number` via the same `format_crossref_number` helper when `ctx.chapter_seed` is `Some` and `ref_type != "sec"`. This reuses the existing helper (no new numbering logic) and is provably safe for the real, non-preview render: `render_resolved_ref` already prefers a pre-set `resolved_number` over recomputing one (that's exactly how it already consumes P5's cross-chapter patches), and for a local resolution the seed is identical at either phase, so the final rendered number for a real book render is unchanged byte-for-byte. Added to the checklist below.
- **Recompute per preview call; no new caching layer, and the "cheap" framing needs an honest caveat.** Both the chapter-seed map and the static crossref sweep are pre-engine operations, but the sweep genuinely goes deeper per file than Pass 1's existing profile sweep does — Pass 1 stops at `DocumentProfileStage`, before `PreEngineSugaringStage` even runs, and Pass 1 itself is IndexedDB-cached across renders rather than naively recomputed. So this phase adds a real, additional, uncached third level of per-file work on a book-preview call (Pass 1's existing shallow sweep; this new, deeper per-sibling sweep; the previewed document's own real render). Accepted for v1 anyway, on the same terms as the epic's existing "unverified at 50+ chapters" memory limitation — fine at small scale, unverified at large scale, revisit with real caching (most likely extending Pass 1's existing IndexedDB cache rather than inventing a new one) only if it's a real complaint, not decided preemptively.
- **`StaticProjectAnalyzer` extracts only what's already syntactically visible pre-engine**, scoped to exactly the files in `BookRenderItem`'s list — not every file `ProjectContext::discover` happens to enumerate (a project can contain non-chapter `.qmd` files, e.g. include-shortcode partials, that must not be swept as if they were chapters). Extracted set matches the single-file crossref design's own D2/D6: native `Figure`/`Table` captions, `#<reftype>-`-classed `Div`s, the code-block `#|label:` shorthand. An id only produced by an engine (`output: asis`, not listed in that chapter's `crossref.ids` manifest) is unresolvable by this sweep — inherited, not new, degradation, matching the existing single-file behavior. **Found by an eighteenth-pass review: the section-counter logic run over each sibling produces only a raw `Order`, not the formatted display string `ProjectCrossrefIndex.resolved_number` requires.** That formatting is design doc §7's `format_crossref_number(order, chapter_seed)` helper — a call `StaticProjectAnalyzer` must make itself (same helper P4/P5 use, not an inline reimplementation of the letter-vs-numeral composition, which would risk drifting from theirs). Check whether `build_analysis_pipeline`/`build_analysis_transform_pipeline` (already Normalization+Crossref-bounded, precedented elsewhere in this epic) can be reused directly for the per-sibling pass before writing new per-file pipeline construction.
- **Duplicate ids across chapters need an explicit merge rule.** P5's own real Pass 3 explicitly plans a duplicate-id-across-chapters diagnostic; the static sweep inherits the identical risk (a naive last-wins merge could resolve a ref against the wrong chapter's content — a correctness bug, not just an approximate number). Rule: first-in-book-order wins, with a non-fatal diagnostic — preview must degrade, never crash, on ambiguous project state. **Note the resulting asymmetry, found by a composition review, not a design fork to resolve**: the identical authoring mistake looks harmless in preview (a soft, non-fatal note) but produces a hard `Error`-severity diagnostic the first time the author runs a real render (P5's mechanism, matching this codebase's existing duplicate-id-is-an-error policy for single documents). This is a deliberate, correct consequence of "preview degrades gracefully, real render doesn't" — but worth one sentence on P7's docs page so it isn't a support surprise: a duplicate id that previews cleanly may still fail a real render.
- **No new resolution mechanism — this reuses P5's `CrossChapterCrossrefResolveTransform` verbatim.** `CrossrefResolveTransform` is **not** modified (an earlier draft of this plan proposed giving it a `project_index` handle directly — reverted; see design doc §12 for why that would have duplicated P5's already-decided mechanism). The only new work is populating the `RenderContext` field P5's transform already reads — `cross_chapter_crossref_registry: Option<Arc<ProjectCrossrefIndex>>` — from `StaticProjectAnalyzer`'s cheaper, pre-engine sweep instead of P5's real post-engine aggregation. Because that transform is registered unconditionally in the shared `build_transform_pipeline` (P5's plan) and preview's own pipeline (`build_q2_preview_transform_pipeline`) builds the *full* shared pipeline before applying a small named deny-list that new transforms join by default, it runs in preview automatically — no preview-specific pipeline change needed. `CrossrefRenderTransform` (which would flatten the resolved node into final markup) is itself excluded from preview regardless, so the `CrossrefResolvedRef` CustomNode reaches React the same way whether it was resolved by real Pass 3 or by this phase's sweep.
- **Resolution precedence is safe by construction, not a rule to implement.** The transform only ever touches nodes `CrossrefResolveTransform` already left unresolved — a previewed chapter's own correct local resolution can never be shadowed by an approximate sibling entry, even under an id collision.
- **Numbers from this sweep are approximate; the link is not.** A number that shifts slightly once fully rendered is a known imprecision, not a bug to chase — no "approximate" confidence-indicator UI is planned for v1 (no Q1 precedent, no evidence of need). `CrossrefResolvedRef`'s `href` is built from `identifier` alone, never `order`, so the approximation never taints navigation correctness, only the displayed number.
- **Content staleness across open editor tabs is not a real concern.** hub-client's Automerge-backed documents are already the live, synced source of truth regardless of tab-open state — the sweep never reads a separate "unsaved buffer" that could be stale relative to what's actually being edited.
- **Cross-document navigation is a small, additive wire-contract change**: one new optional `plain_data` field, `owning_chapter_path` (a project-relative source path, e.g. `"chapters/ch2.qmd"` — deliberately *not* `owning_chapter_href`, which is the rendered *output* href P5's real Pass 3 uses and means nothing to hub-client's document-based navigation), present only for cross-chapter refs (absent/unchanged for same-page refs, so existing non-book preview is unaffected — regression-tested below). Threaded via a new optional field on `ProjectCrossrefEntry`/`ChapterCrossrefInventory` (defaults to `None`; P5's real Pass 3 aggregation call site in `multi_file_html.rs` never sets it, so real book renders are unaffected — the field is inert there since `CrossrefRenderTransform` flattens the node away before anything would read it). `CrossrefResolvedRef.tsx` calls the already-threaded, currently-unused `onNavigateToDocument` prop when that field is present, instead of the current unconditional same-page `href="#{identifier}"`.
- **Explicitly out of scope, not silently dropped**: cross-chapter bibliography preview (no pre-engine analog exists to P6's per-chapter citeproc manifest — citation resolution isn't syntactically visible before engines run for most CSL styles) and single-file-merge preview (preview always renders as plain HTML regardless of the book's real single-file target format; P2/P3's merge has nothing to preview here). Both stay listed in the design doc's §11/epic's Known Limitations, narrowed rather than removed.
- **Found during implementation (2026-09-25): `RenderToHtmlRenderer` (the WASM hub-client renderer for non-`q2-preview`/`q2-slides` formats, `render_project_active_page_to_response`'s `_ =>` branch) deliberately gets none of this phase's wiring** — no `chapter_seeds` field, no `StaticProjectAnalyzer` call. This plan's scope is `RenderToPreviewAstRenderer` specifically (per the Overview: the AST-based, React-rendered `q2-preview` path), not every hub-client render path. `RenderToHtmlRenderer` still uses `Pass2Renderer::install_book_chapter_seeds`'s no-op default, unchanged from before this phase.
- **Found during implementation (2026-09-25): `ctx.diagnostics` (the outer `RenderContext` field) is not bridged into the pipeline `RenderToPreviewAstRenderer::render` drives.** `stage_context_from_render_context` (pipeline.rs) builds a fresh `StageContext` whose `diagnostics` starts from extension-discovery output only — it never reads the caller's `ctx.diagnostics`. So `StaticProjectAnalyzer`'s sweep diagnostics (unreadable sibling, duplicate id across chapters) are collected into a local `Vec` inside `render()` and merged into `preview_output.diagnostics` *after* `render_qmd_to_preview_ast` returns, not routed through `ctx.diagnostics` before the call (that would silently drop them).

## Checklist

### Tests first
- [x] Regression test: previewing a chapter in a *non-book* project is byte-for-byte unaffected by this phase's changes — no chapter seed applied, `cross_chapter_crossref_registry` stays `None` (P5's own no-op guard covers this; confirm it holds through the preview path too), `CrossrefResolvedRef`'s `plain_data` gains no new field for a same-page ref. Landed as `book_preview.rs`'s `non_book_preview_is_unaffected_by_book_preview_wiring`.
- [x] Test: previewing chapter 2 of a real multi-chapter book fixture shows chapter-scoped numbering ("Figure 2.1", not "Figure 1") and chapter-title decoration ("Chapter 2 — Title"), matching what a real `BookProjectType` render of that same chapter would produce. **Scope note found while writing this test**: `crossref-render` (the transform that renders the literal `"Figure\u{a0}2.1"` text / `header-section-number` span / "Chapter N —" decoration) is on `Q2_PREVIEW_TRANSFORM_EXCLUDED` — it never runs in preview, by design (preview needs the still-structured `CrossrefResolvedRef` node). So this test inspects the structured data those renderings are built from instead, one layer earlier: `resolved_number` ("2.1") on the crossref-resolved node (what `CrossrefResolvedRef.tsx` actually reads to build its display text), and the `number` kv `CrossrefIndexTransform` stashes on the `Header` block itself (`["number","2"]` — the same fact a real render's title decoration is built from; no React component consumes this kv today, since nothing in this phase's scope adds book-chapter heading decoration to the preview UI). Landed as `book_preview.rs`'s `book_chapter_preview_gets_chapter_scoped_number_and_header_number_kv`.
- [x] Test (`crossref_resolve.rs`): a same-chapter (local) figure/table/equation resolution with a `ChapterSeed` present gets `resolved_number` composed at Crossref-phase resolution time (e.g. `"2.1"` for chapter 2's first figure); a `sec`-type local resolution gets no `resolved_number` (unaffected — its raw `order.section` is already chapter-scoped). A local resolution with no `ChapterSeed` (non-book) gets no `resolved_number`, matching today's behavior exactly. Landed as three tests: `local_resolution_composes_chapter_scoped_number_when_seeded`, `local_sec_resolution_gets_no_composed_number`, `local_resolution_without_seed_gets_no_composed_number`.
- [x] Regression test: a real (non-preview) book render's final HTML output is byte-for-byte unchanged by the local `resolved_number` fix above — `render_resolved_ref`'s Finalization-phase composition and the new Crossref-phase composition must produce the identical display string for the same local resolution. Landed as `crossref_render.rs`'s `local_figure_ref_renders_identically_whether_number_is_composed_early_or_late`, run through the real full pipeline (`run_full_opts`: Resolve → Render) — confirms "Figure 2.1" end to end, not just the intermediate plain_data value.
- [x] Test: an `.unnumbered` chapter previews with no chapter-number seed applied (matches P0/P4's existing unnumbered-chapter behavior, exercised here through the preview path specifically). Landed as `book_preview.rs`'s `unnumbered_chapter_preview_gets_no_seed_or_number_kv`.
- [x] Baseline test: before this phase's crossref-resolution mechanism is wired up, a cross-chapter `@ref` in preview degrades to the existing unresolved-placeholder rendering (`?id?`), not a crash — pins today's starting behavior so the improvement is measurable. Landed as `book_preview.rs`'s `preview_unknown_cross_chapter_ref_stays_unresolved` (an id no chapter defines anywhere resolves to `resolved: false`, no crash).
- [x] Test: `StaticProjectAnalyzer` resolves a cross-chapter `@ref` to the target chapter's approximate kind/number when the target is a statically-visible crossref (native figure/table caption, `#<reftype>-`-classed Div, `#|label:` shorthand) in another chapter, with that chapter's own `ChapterSeed` correctly applied so the number is chapter-scoped. Landed as `book_preview.rs`'s `static_analyzer_resolves_cross_chapter_ref_with_owning_chapter_path` (ch2's `@fig-one` resolves to ch1's scoped `1.1`).
- [x] Test: an `@ref` to an engine-generated (`output: asis`, un-promised) id in another chapter still degrades to the unresolved-placeholder rendering, not a crash or an incorrect resolution. Landed as `book_preview.rs`'s `engine_generated_id_in_sibling_degrades_gracefully`.
- [x] Test: two chapters independently defining the same crossref id produce a non-fatal diagnostic and a deterministic (first-in-book-order) resolution in the static sweep, not a silent last-wins overwrite and not a preview crash. Landed as `book_preview.rs`'s `duplicate_id_across_chapters_is_non_fatal_and_first_chapter_wins` (a third chapter's cross-chapter ref resolves against the first chapter defining the id; a `Q-15-2` diagnostic surfaces in `preview_output.diagnostics` and the render still succeeds).
- [x] Test: a non-chapter `.qmd` file present in the project directory but absent from `BookRenderItem`'s list is not swept by `StaticProjectAnalyzer` and cannot be resolved as a cross-chapter target. Landed as `book_preview.rs`'s `non_chapter_qmd_file_is_not_swept_by_static_analyzer`.
- [x] Regression test: `CrossChapterCrossrefResolveTransform` (P5) does not accidentally resolve a previewed chapter's own crossref id against a stale/approximate sibling entry — same-chapter local resolution always wins. Landed as part of `book_preview.rs`'s `duplicate_id_across_chapters_is_non_fatal_and_first_chapter_wins` (ch2's own local `@fig-dup` resolves to its own `2.2`, never ch1's `1.2`, and carries no `owning_chapter_path`).
- [x] Test: the new `plain_data` field is present (with the correct owning-chapter path) for a cross-chapter-resolved ref, and absent for a same-chapter-resolved ref. Covered on the Rust side by the two tests above (present + `ch1.qmd` for the cross-chapter case; absent for the local case) and on the TS side by the React test below.
- [x] `schemaConformance.test.ts` extended (both directions) to cover the new field, per the existing convention for `CrossrefResolvedRef`'s key array. **This requires a matching entry in `crates/quarto-pandoc-types/resources/custom-node-schema.json`'s `CrossrefResolvedRef.plain_data` block, not just the `.tsx` key array** — found by an implementability review: T4.3 in `schemaConformance.test.ts` strictly checks the TS component's key array against this schema file's keys (`toEqual` on sorted arrays), so extending only `CROSSREF_RESOLVED_REF_PLAIN_DATA_KEYS` without a corresponding schema entry makes that exact test fail, not pass. Mirror `order`'s existing entry shape. **No new test code needed**: T4.3 is generic (iterates every schema-reachable type's key array against the schema, no per-field assertions), so it already covers any key added to both `CROSSREF_RESOLVED_REF_PLAIN_DATA_KEYS` and the schema file — verified passing (10/10) with the three P8 keys already present on both sides from the Implementation section's landed work.
- [x] React test: `CrossrefResolvedRef` calls `onNavigateToDocument` with the correct target when the new field is present, and renders the existing same-page `href` unchanged when it's absent. Landed as `CrossrefResolvedRef.test.tsx` (3 tests): same-chapter resolution keeps `href="#id"` and never calls `onNavigateToDocument`; cross-chapter resolution keeps the same `href` but calls `onNavigateToDocument('ch1.qmd', 'fig-one')` on click; an unresolved ref renders `"?id?"` and never calls the navigation callback either.

### Implementation
- [x] `crossref_resolve.rs`: thread `ctx.chapter_seed` down into `build_resolved_ref` (via `resolve_blocks`/`resolve_block`/`resolve_inlines`/`resolve_inline`/`classify_cite`); when resolved and `ref_type != "sec"` and a seed is present, insert `resolved_number` via `format_crossref_number` (imported from `crossref_render.rs`, already `pub(crate)`). Landed; `cargo clippy -p quarto-core --all-targets -- -D warnings` and `cargo nextest run -p quarto-core -E 'test(crossref)'` both clean (260/260).
- [x] `RenderToPreviewAstRenderer` gains the full per-chapter seed map (built the same way P4 builds it), applied to the previewed document via the equivalent of P4's `with_chapter_seeds` builder. Landed by giving `RenderToPreviewAstRenderer` its own `chapter_seeds: HashMap<PathBuf, ChapterSeed>` field (mirroring `RenderToFileRenderer`) plus a real `install_book_chapter_seeds` override (was silently using the trait's no-op default); `render()` looks up `doc_info.input` and sets `ctx.chapter_seed` directly (no options-clone needed — q2-preview builds its `RenderContext` inline). No orchestrator changes needed: `ProjectPipeline::run_inner` already calls `install_book_chapter_seeds` generically after `pre_render` for every renderer, gated on `project.book_render_items.is_some()` — this resolves the handoff's open question about a book-detection accessor, since `book_render_items` is `Some` only for `ProjectKind::Book` after `pre_render`, which is exactly the signal needed (and the render-items list itself, which the seed map is built from). `cargo clippy -p quarto-core --all-targets -- -D warnings` clean; `cargo nextest run -p quarto-core -E 'test(crossref) or test(chapter_seed) or test(book)'` → 376/376 passed.
- [x] New `StaticProjectAnalyzer` module: pre-engine sweep of `BookRenderItem`-listed sibling files' parsed AST — check whether `build_analysis_pipeline`/`build_analysis_transform_pipeline` can be reused directly before writing new per-file pipeline construction — producing a `ProjectCrossrefIndex` (P5's merge type), with the seed map from above applied per sibling, `format_crossref_number(order, chapter_seed)` (design doc §7) called explicitly to produce each entry's `resolved_number` string, and the duplicate-id merge rule applied during aggregation. Landed as `crates/quarto-core/src/project/book/static_analyzer.rs` (`pub(crate) async fn analyze_book_project_statically`): confirms `build_analysis_pipeline` fits verbatim (per the LSP-precedented `analyze_document_async` template) — no new per-file pipeline construction needed. Per sibling: reads via `runtime.file_read` (VFS-safe for WASM), keys the seed lookup with the exact same `Path::canonicalize`-or-fallback expression `chapter_seed_map` uses (so lookups hit), bridges the seed into `stage_ctx.chapter_seed` before running the pipeline, then harvests `stage_ctx.crossref_index` after. Builds one `ChapterCrossrefInventory` per sibling with `owning_chapter_path: Some(<project-relative source path>)` and `output_href: String::new()` (inert placeholder — preview has no real rendered output; see the next item and the `owning_chapter_path` field addition below), then hands the list to `aggregate_chapter_inventories` unmodified — no reimplemented merge or number-composition logic. Added `owning_chapter_path: Option<String>` to `ProjectCrossrefEntry`/`ChapterCrossrefInventory` (defaults `None`; `multi_file_html.rs`'s real Pass 3 call site now sets it explicitly to `None`) and patched `cross_chapter_crossref_resolve.rs`'s `resolve_node` to copy it into the resolved node's `plain_data` when present (mirrors the existing `order`/`in_appendix` insert pattern) — this is the P5-side half of the plan's `owning_chapter_path` decision, landed alongside the analyzer since the analyzer is what actually populates the field.
- [x] Populate `RenderContext.cross_chapter_crossref_registry` (P5's field) from the `StaticProjectAnalyzer` sweep in the preview render path only; confirm no other call site sets it, and confirm P5's transform requires no changes to consume it. Wired into `RenderToPreviewAstRenderer::render` (pass2_renderer.rs): gated on `project.book_render_items.is_some()` (same gate as the chapter-seed map, so non-book preview is provably untouched — the field stays `None`), sets `ctx.cross_chapter_crossref_registry = Some(Arc::new(registry))`. No pipeline changes needed to consume it: `stage_context_from_render_context` (pipeline.rs:1031) already bridges `ctx.cross_chapter_crossref_registry` into `stage_ctx` unconditionally for every `RenderContext`-based render (P5 infrastructure, unchanged), and `CrossChapterCrossrefResolveTransform` is already registered unconditionally in the shared pipeline P5 built. Sweep diagnostics are collected separately and merged into `preview_output.diagnostics` after the render call — `ctx.diagnostics` itself isn't bridged into this pipeline's `stage_ctx.diagnostics` (`StageContext::new` seeds diagnostics from extension-discovery only), so a diagnostics-only field set on `ctx` before the call would have been silently dropped. `cargo clippy -p quarto-core --all-targets -- -D warnings` clean; `cargo nextest run -p quarto-core -E 'test(crossref) or test(chapter_seed) or test(book) or test(preview)'` → 446/446 passed (1 leaky, pre-existing/unrelated — a process/thread-leak warning, not a failure).
- [x] New optional `plain_data` field on `CrossrefResolvedRef` — TS reader in `CrossrefResolvedRef.tsx`, plus a matching entry in `crates/quarto-pandoc-types/resources/custom-node-schema.json`. **Rust-side citation corrected by an implementability review**: `crossref_resolve.rs`'s `build_resolved_ref` only builds the *local, same-page* resolution — the cross-chapter mutation that actually sets this field belongs wherever P5's `CrossChapterCrossrefResolveTransform` is implemented (not yet written; that file doesn't exist until P5 lands), reusing the same `serde_json::Value::Object`-mutation idiom `crossref_resolve.rs` already uses for `order`. Rust side landed already (previous checklist item, alongside `StaticProjectAnalyzer`). This item is the TS+schema half: added `resolved_number`, `target_href`, and `owning_chapter_path` together to `CROSSREF_RESOLVED_REF_PLAIN_DATA_KEYS` and to `custom-node-schema.json`'s `CrossrefResolvedRef.plain_data` block (mirroring `order`'s shape) — all three needed together since `resolved_number`/`target_href` were *already* written by Rust (P5) but missing from both TS-side sources, a latent gap the implementability review flagged; T4.3's strict sorted-key-array equality would fail on any one of the three being added alone. `npm run typecheck` clean; `schemaConformance.test.ts` → 10/10 passed; `cargo nextest run -p quarto-core -E 'test(custom_node_schema_conformance)'` → 19/19 passed (confirms the new optional schema keys don't disturb the Rust-side per-branch `assert_keys_equal` checks, since none of this test's fixtures produce a `ChapterSeed` or cross-chapter registry).
- [x] `CrossrefResolvedRef.tsx`: branch on the new field, calling `onNavigateToDocument` instead of the bare same-page `href` when present. Landed as an `onClick` handler (not a swapped `href`, so the anchor stays keyboard/middle-click affordant): present only when `owning_chapter_path` is set, calls `event.preventDefault()` then `onNavigateToDocument(owningChapterPath, identifier)` — signature confirmed from `framework/types.ts:151` (`(path: string, anchor: string | null) => void`); `href="#{identifier}"` is unchanged for the absent case (same-chapter resolution, and every real non-preview book render). Also fixed the pre-existing bug this phase's own Decisions flagged: the component was reading `plain.order.order` directly for the displayed number, ignoring the new `resolved_number` field entirely — `displayNumber` now prefers `resolved_number` (chapter-scoped composed string) and falls back to `order.order` only when absent (non-book resolution — today's behavior unchanged). `npm run typecheck` and `vitest run` (non-integration suite) clean: 609/609 passed, 0 failures.
- [x] `cargo clippy -p quarto-core --all-targets -- -D warnings`; `cargo nextest run -p quarto-core`; `npm run build --if-present -w ts-packages/preview-renderer`; phase-boundary `cargo nextest run --workspace` with delta reported. All green: `cargo clippy -p quarto-core --all-targets -- -D warnings` clean; `cargo nextest run -p quarto-core` → 5072/5072 passed (31 skipped, 0 failed — up from the 446/19 filtered-subset baseline once `book_preview.rs`'s 8 new tests and the rest of the crate are included in an unfiltered run); `npm run build --if-present -w ts-packages/preview-renderer` (`tsc`) clean; `npx vitest run --exclude "**/*.integration.test.tsx"` → 612 passed (up from 609 — the 3 new `CrossrefResolvedRef.test.tsx` tests), 36 skipped, 0 failed. Phase-boundary `cargo nextest run --workspace` → **14925 passed (1 slow), 200 skipped, 0 failed** — this is the first workspace-wide run this phase (none had been captured yet at handoff), so there is no same-phase prior number to diff against; the 0-failure result is itself the gate.

## Details

**End-to-end verification (this repo's standing rule):** exercise this through a real hub-client preview session (or, at minimum, the WASM-level integration tests already used for the non-book preview path) against a real multi-chapter book fixture with a cross-chapter reference — inspect the previewed chapter's rendered number, the cross-chapter link's `href`/navigation behavior, and confirm a non-book project's preview is unchanged. Record the exact fixture and what was inspected in this checklist as each item completes.

**Dependency note**: this phase depends on P5's `CrossChapterCrossrefResolveTransform` and `RenderContext.cross_chapter_crossref_registry` field existing and being registered in the shared `build_transform_pipeline` exactly as P5's plan now specifies (see that plan's Decisions) — confirm P5 has landed in that shape before starting this phase's implementation, not just that P5 is "done" in name.
