# Observed behavior at main @ 0c3542d0 (2026-08-18)

Fixture: `repro.qmd` (same directory).

## q2 (pampa native writer)

```
$ cargo run -q -p pampa --bin pampa -- repro.qmd -t native
[ Header 2 ( "hello" , [] , [("id", "get-/v1/x")] ) [Str "Hello"],
  Para [Span ( "" , [] , [("id", "sp"), ("class", "c1 c2")] ) [Str "span"]],
  Div ( "" , [] , [("id", "dv")] ) [Para [Str "content"]],
  Para [Code ( "" , [] , [("id", "cd")] ) "code"],
  Header 2 ( "short" , [] , [] ) [Str "Ctl"] ]
```

The kv `id`/`class` stay in the key-value map (attr.2); the identifier
slot (attr.0) stays empty, so the heading additionally receives the
auto-generated slug `hello`. Sectionized HTML then emits both as `id=`,
producing a duplicate-attribute parse error.

## pandoc 3.x (authority)

```
$ pandoc -f markdown repro.qmd -t native
[ Header 2 ( "get-/v1/x" , [] , [] ) [ Str "Hello" ]
, Para [ Span ( "sp" , [ "c1" , "c2" ] , [] ) [ Str "span" ] ]
, Div ( "dv" , [] , [] ) [ Para [ Str "content" ] ]
, Para [ Code ( "cd" , [] , [] ) "code" ]
, Header 2 ( "short" , [] , [] ) [ Str "Ctl" ]
]
```

## pandoc edge-case probes

```
## A {#short id="kv"}      -> Header ( "kv", ... )      (last id wins)
## B {id="kv" #short}      -> Header ( "short", ... )   (last id wins)
## C {id="one" id="two"}   -> Header ( "two", ... )     (last id wins)
## D {class="x" .y class="z"} -> Header ( "d", ["x","y","z"], [] )
                                 (classes accumulate in source order;
                                  kv class values split on whitespace;
                                  auto-id still generated when no id)
```
