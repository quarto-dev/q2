# Plan: Typst + EPUB book output, DOCX/PPTX scope-out (book-projects P3)

**Date:** 2026-09-21
**Epic:** [`2026-09-21-book-projects-epic.md`](2026-09-21-book-projects-epic.md)
**Design (authoritative):** [`../designs/book-projects-architecture.md`](../designs/book-projects-architecture.md) §6, §11
**Q1 ground truth:** `tests/docs/smoke-all/typst/orange-book` (and `orange-book-lang`/`orange-book-margin`/`override-orange-book`) — the deepest numbering-correctness fixture in Q1's own test suite, with machine-checked `.typ`-source and PDF-text assertions covering every crossref-numbered construct type.

## Overview

Wire P2's merge mechanism through the two real single-file book formats end-to-end, and make the currently-silent "book doesn't support docx/pptx" gap into an explicit diagnostic instead. This phase is mostly wiring — P2 already did the hard part (a format-agnostic merge that hands off to the unmodified single-document pipeline).

## Decisions

- **Chapter-scoped numbering and appendix-lettering for Typst are not "already vendored and working" the way brand/CSS/templates are** — they depend on the Typst book extension's own filter (`orange-book.lua` or a sibling), not on Quarto's generic vendored Lua (see design doc §5, corrected during review: an earlier draft of P2 mis-cited `book-cleanup.lua`'s own comment, which is wrong). P2 vendors `orange-book` locally as a test fixture and emits the comment markers it needs; this phase is where that fixture actually gets exercised end-to-end through a real Typst compile, not assumed to "just work" because the rest of the Typst pipeline already does.
- Full Q1 feature parity is the target for Typst (matching `feature/pandoc-writer-hybrid`'s own typst plan's stated end state) — brand/CSS mapping, templates, packages are already vendored and working for single documents; this phase just confirms they work unchanged for a *merged* document.
- EPUB gets chapter splitting via `--split-level` automatically once the merged document has real per-chapter H1s — no book-specific EPUB code beyond confirming the split boundary matches book chapter boundaries (not, e.g., every H1 including sub-chapter headings if `epub-chapter-level` differs from 1).
- **`epub-cover-image` derivation**: when `book.cover-image` is set and no explicit `epub-cover-image` is present, derive one from the other. **Corrected by a ground-truth-verification pass — this is not actually a "direct port" of `epubBookExtension.onSingleFilePreRender`, though the target is still the right one for Q2's architecture.** Reading `format-epub.ts` directly: Q1's function writes `format.metadata[kBookCoverImage]` (`kBookCoverImage = "cover-image"`, a *generic* Pandoc metadata key that Pandoc's own EPUB writer reads natively) — it never writes `format.pandoc["epub-cover-image"]` anywhere in Q1's codebase. Q2 has no equivalent generic `cover-image` pass-through for EPUB; the already-landed `feature/pandoc-writer-hybrid` work only forwards a key literally named `epub-cover-image` (as a `--epub-cover-image=` CLI flag). So P3's mechanism is a deliberate *adaptation* to Q2's existing architecture — derive Q2's `epub-cover-image` key from `book.cover-image` — not a literal port of Q1's mechanism, which targets a different, pandoc-native metadata field Q2 doesn't special-case. Don't go looking for a `cover-image`-keyed mechanism to port; there isn't one to reuse.
- DOCX/PPTX book targets: explicit diagnostic via P1's general `BookProjectType::is_supported_format` rule (port of Q1's `projectFormatsOnly`/`isSupportedFormat`) — not a one-off special case for these two formats, and not a silent per-chapter fallback (Q1's own default behavior there is arguably a footgun, not a feature worth reproducing).
- LaTeX/PDF: not implemented (out of scope per epic), but P2's `top-level-division: chapter` lever was deliberately built generically so this is a small follow-on later, not a redesign.

## Checklist

### Tests first
- [x] Reduction of `orange-book`'s numbering torture-test (figures, tables, equations, theorems/lemmas/definitions, listings, labeled callouts, sub-figure panels, a custom `crossref.custom` kind) rebuilt as a Q2 fixture, rendered via the real `q2 render <book> --to typst` end-to-end path against the *unpatched*, locally-vendored `orange-book` extension (P2's fixture — this phase is where it's actually exercised, not P2), asserting chapter-scoped numbers ("Figure 1.1", "Figure 2.1" resetting per chapter, "Theorem A.1" for appendices, "Figure 2.1a"/"2.1b" for sub-figure panels) against the compiled PDF text or intermediate `.typ` source.
  - `crates/quarto-core/tests/integration/book_numbering_torture.rs` (`orange_book_numbering_torture_reduces_q1_smoke_fixture`), driven through `ProjectPipeline::run_with_book_support()` — the same path the CLI takes. Verified RED at the listing-reference assertions, then GREEN.
  - **E2E record (probe render that preceded the test, `cargo run --bin q2 -- render .tmp-torture-probe --to typst`):** compiled `Torture-Probe.pdf` inspected via `pdf_extract`; every expected string present — "Figure 1.1: A plot of the cars dataset", "Warning 1.1: Whose Tea Is This?", "Dinosaur 1.1", "Theorem 1.1 (Pythagorean Theorem)", "Lemma 1.1 (Triangle Inequality)", "Listing 1.1: Hello World in Python", "Equation (1.1)"; chapter two resets — "Table 2.1", "Definition 2.1 (Continuous Function)", "(a) First panel"/"(b) Second panel", "Figure 2.1a"/"Figure 2.1b", "Equation (2.1)", "!contains Figure 1.2"; cross-chapter — "Refer back to Figure 1.1 and Equation (1.1) from Chapter One."; appendix — "A. Appendix Alpha", "Theorem A.1 (Appendix Theorem)", "Figure A.1", "Warning A.1", "Equation (A.1)", "!contains Theorem 3.1"/"3. Appendix Alpha".
  - **Discovered by this test: the attribute-form listing (` ```{#lst-x .python lst-cap="…"} `) was never crossref-indexed** — its FloatRefTarget was created only by the vendored Lua (`parsefiguredivs.lua`), which runs *after* `CrossrefIndexTransform`, so captions numbered ("Listing 1.1: …") but `@lst-x` in-text references failed with "no target with this identifier was found". New item below.
- [x] Test: a cross-chapter reference within the merged single-file document (e.g. chapter 3 referencing a chapter 1 figure) resolves to the original chapter's number — this is "free" per §6 but must be asserted, not assumed.
  - Asserted inside the torture test ("Refer back to Figure 1.1 and Equation (1.1) from Chapter One." — chapter two referencing chapter one's constructs).
- [x] **Attribute-form listing code blocks (` ```{#lst-x lst-cap="…"} `) sugar in Rust** (discovered by the torture test): `FloatRefTargetSugarTransform` gains a `CodeBlock` arm — lst-only, `lst-cap`-required, id moved to the node + inner code block id cleared, caption parsed as markdown — mirroring Q1's `parsefiguredivs.lua` `CodeBlock` handler, so the target exists *before* `CrossrefIndexTransform` and `@lst-*` references resolve in every format. Unit tests in `float_ref_target.rs` (converts; no `lst-cap` → untouched; non-`lst` id → untouched).
  - Landed in `crates/quarto-core/src/transforms/float_ref_target.rs`: `classify_codeblock` (lst-refType + `lst-cap` required) + `convert_codeblock` (inner code block loses id and `lst-cap`, keeps classes/attrs; caption via `parse_config_string_as_markdown`; `caption_long` pre-canonicalized). 3 unit tests green; full per-crate gate green (clippy clean, `cargo nextest run -p quarto-core` 5018 passed / 31 skipped); torture test now resolves "See Listing 1.1 …" / "See Listing 2.1 …" in the compiled PDF.
- [x] Test: EPUB book render produces one `.epub` with chapters split at `book.chapters` boundaries; unzip and inspect `EPUB/text/*.xhtml` count and content, mirroring `feature/pandoc-writer-hybrid`'s own `e2e_render_epub_chapter_level` methodology.
  - `crates/quarto-core/tests/integration/book_epub.rs::epub_book_splits_chapters_at_book_chapter_boundaries` — 3 chapter `.xhtml` files under `EPUB/text/`, each chapter's body in a *different* file (merged-no-split regression guard). GREEN.
- [x] Test: both Typst's and EPUB's unified bibliography (one merged reference list *and* correct in-text citation numbers, not per-chapter) — built by P2's citeproc-deferral fix (design doc §6), not automatic (an earlier draft of this epic wrongly assumed it was free; see the epic doc's revision history). Assert it end-to-end for both formats, don't assume it.
  - Typst leg: P2's `book_citations.rs` suite already covers this comprehensively (book-wide numbering, year-suffix disambiguation, normalization-before-deferred-citeproc) — no gap found; this phase adds only the EPUB leg.
  - EPUB leg: `book_epub.rs::epub_book_bibliography_is_unified_across_chapters` — ch1 cites `[@knuth1984]`+`[@doe2020]` → "[1]"+"[2]"; ch2 re-cites `[@knuth1984]` → still "[1]" (book-wide, not per-chapter); no "[3]" anywhere; "The TeXbook" appears exactly once across all split chapter files (one merged bibliography). GREEN.
- [x] Test: `book.cover-image` set, no explicit `epub-cover-image` — the rendered `.epub`'s OPF manifest marks the derived cover with `properties="cover-image"`.
  - `book_epub.rs::book_cover_image_derives_epub_cover_image_opf_property` GREEN. RED-state verified: with the derivation temporarily disabled the test fails (the `cover-image` marker comes from the derivation wiring, not from `cover.png`'s presence as a media file).
  - Implementation (item below) landed in `single_file_render.rs` post-merge: EPUB-only, `book.cover-image` → top-level `epub-cover-image` (project-root-relative after stripping a leading `/`), skipped when the author set `epub-cover-image` explicitly. The consumer (`pandoc_write.rs::epub_extra_args`) resolves it doc-relative against the synthetic merged input, whose doc dir *is* the project root.
- [x] Test: `q2 render <book> --to docx` produces a clear diagnostic naming the unsupported combination, not a silent per-chapter render and not a panic.
  - `crates/quarto/tests/integration/book_docx_diagnostic_e2e.rs::e2e_book_to_docx_fails_with_q_5_33_diagnostic` — drives the real `q2` binary (`CARGO_BIN_EXE_q2`): non-zero exit, no panic, `Q-5-33` + the named target format in the output, no `.docx` produced. GREEN.
  - **E2E record (manual probe, `cargo run --bin q2 -- render .tmp-unnum-app-probe --to docx`):** exit 1, stderr exactly —
    `Error [Q-5-33]: Book projects cannot render to `docx`` /
    `This book project targets the `docx` format, which has no book support. Book projects render only to `html`, `typst`, and `epub`.` /
    `ℹ Render to a supported format (`q2 render --to html`), or change the project's `format:` key.`
- [x] **Integration test, compiled-artifact half of P2's cross-artifact invariant check** (ownership split clarified by a review pass — P2 tests the data-level invariant; this is the half that needs a real compile, which is this phase's job): for a fixture with an appendix chapter, the appendix letter P1 computed for the sidebar (via P4's multi-file path, or by inspecting the sidebar structure directly) matches the appendix letter `orange-book` actually produced in the compiled single-file Typst PDF for that same chapter. This closes design doc §4's "provably equal by construction, but nothing tests it" note.
  - `crates/quarto-core/tests/integration/book_appendix_letter_parity.rs::sidebar_appendix_letters_match_the_compiled_typst_pdf_letters` — two numbered appendices; the data half calls P1's `book_render_items` + `chapter_label_prefix` ("A"/"B"); the PDF assertion is written in terms of the *computed* letters, so a regression on either side breaks a different half. GREEN ("A. Appendix Alpha" / "B. Appendix Beta" in the extracted PDF text).
- [x] **Integration test, the invariant's second half — added by a ground-truth-verification pass that found the design doc's original premise false**: a fixture with an `.unnumbered` individual appendix chapter (not the synthetic "Appendices" divider — a real appendix chapter, unnumbered on its own), rendered through single-file Typst mode. Confirm whether `orange-book`'s appendix show-rule skips it the same way `sections.lua` skips an unnumbered ordinary chapter for numeric numbering (design doc §4). If it does, this is a straightforward regression guard. If it *doesn't* — i.e. the unpatched extension still assigns it a letter — that's a real, previously-unknown Q1-fidelity gap this epic needs a decision on (diagnostic, documented limitation, or a fix), not something to leave silently discovered by this test alone.
  - `book_appendix_letter_parity.rs::unnumbered_appendix_chapter_in_single_file_typst` — documenting test, GREEN. **Probed outcome (2026-09-24): the unpatched extension DOES skip it** — the unnumbered appendix's heading renders bare (no letter), and the *next* numbered appendix receives letter **"A"** (the extension letters by counting rendered appendix headings).
  - **Finding surfaced to Gordon (decision needed, not blocking):** the sidebar data path — a faithful port of Q1's `chapterInfoForInput` — letters that same next-numbered appendix **"B"** (unnumbered consumes no slot; numbered appendices stay dense 1=A, 2=B). So for this corner case the sidebar and the compiled PDF genuinely diverge — and both halves are Q1-faithful ports, meaning **Q1 itself has this sidebar-vs-PDF divergence**; Q2 reproduced it faithfully. This is design doc §4's "cross-artifact risk" with concrete evidence. Options later: accept as Q1-faithful (status quo, documented), or deviate from Q1 by keying sidebar letters off rendered-heading counts. Not a P3 decision.
  - **Side observation (out of scope):** the unpatched `@preview/orange-book:0.7.1` `lib.typ:584` crashes at compile time for a book with **no `author:`** (`expected content, found array`) — identical in Q1, upstream extension behavior; fixtures here set an author.
- [x] **Resolved with the user (fifteenth pass), no longer an open test/decision**: a Typst book target with no `format.typst` extension configured now auto-resolves to the vendored `orange-book` (P2's Decision, mirroring Q1's own zero-config default) rather than silently degrading — the diagnostic-vs-documenting-test fork this item used to pose no longer applies to the default case. P2 owns the auto-default mechanism and its own end-to-end test (a book with no extension configured renders/compiles successfully via `orange-book`); this phase's job is to confirm that test actually exercises a real `--to typst` invocation end-to-end (not just the merge/attribute-stamping mechanism P2 tests directly) as part of this phase's own end-to-end verification pass.
  - **Confirmed.** `with_typst_book_default` (`extension/discover.rs:296`) is wired into the real pipeline at `metadata_merge.rs:100` (not just unit-tested), and the zero-config fixtures prove the whole leg: `book_part_appendix.rs` and `book_numbering_torture.rs` both render a book whose `_quarto.yml` contains **no `format:` key at all** through `run_with_book_support()` to a real compiled PDF — and assert orange-book-only artifacts (`#part[Part One]` in the kept `.typ`; "Theorem A.1"/"Figure A.1" chapter-scoped numbering in the PDF text). If the auto-default did not fire through the real `--to typst` path, those artifacts could not exist. No new test needed.

### Implementation
- [x] `run_with_book_support()`'s single-file-merge branch (design doc §3) dispatches to Typst's existing `TypstCompileStage`/`PandocWriteStage` unchanged, passing the merged document.
  - Landed in P2 (`run_book_single_file_merge`, `orchestrator.rs:1042` for the Typst/Epub branch); this phase's entire test suite is the proof the dispatch works through real compiles — the torture test compiles a real PDF through it, `book_part_appendix.rs` does the same with a part divider, and `book_appendix_letter_parity.rs` extracts letters from that PDF.
- [x] Same for EPUB's existing `PandocWriteStage` path.
  - Same dispatch branch; proven by `book_epub.rs`'s three tests (chapter split, unified bibliography, derived cover) plus the CLI probe: `cargo run --bin q2 -- render .tmp-unnum-app-probe --to epub` → `_book/Unnum-App-Probe.epub`; unzipped and inspected — `EPUB/text/ch001..ch004.xhtml` (one per chapter) with chapter bodies and `data-quarto-book-item-*` metadata attributes on headings (e.g. `<h1 class="unnumbered" data-quarto-book-item-type="chapter" data-quarto-book-item-file="app-a.qmd" data-quarto-book-item-appendix="true">Unnumbered Appendix</h1>`).
- [x] `book.cover-image` → `epub-cover-image` derivation, port of `epubBookExtension.onSingleFilePreRender`.
  - Landed in `single_file_render.rs` post-merge (EPUB-only; explicit `epub-cover-image` wins; leading `/` stripped per the path-resolution contract; synthetic merged input's doc dir == project root). See the test item above for the RED→GREEN record.
- [x] Diagnostic for DOCX/PPTX book targets (new or extended error-catalog entry; check whether `Q-16-7` fits or a new book-specific code is clearer).
  - Landed in P1/P2: `run_with_book_support()` gates known-but-unsupported identifiers via `book_diagnostic("Q-5-33", …)` (`orchestrator.rs:1023`), naming the combination ("Book projects cannot render to `docx`" + the supported-format list). `Q-5-33` reused — no new code.
- [x] `docs/errors/<subsystem>/<code>.qmd` page for any new diagnostic code, per this repo's `error-docs-page-missing` lint.
  - No new code introduced — `Q-5-33` was reused and its page already exists (`docs/errors/project/Q-5-33.qmd`). Item closes as a no-op.
- [x] `cargo clippy -p quarto-core --all-targets -- -D warnings`; `cargo nextest run -p quarto-core`; phase-boundary `cargo nextest run --workspace` with delta reported.
  - All green (2026-09-24): clippy clean for `-p quarto-core` and `-p quarto`; `cargo nextest run -p quarto-core` 5023 passed / 31 skipped; **phase-boundary `cargo nextest run --workspace` 14876 passed / 200 skipped** — delta vs live baseline **14866 passed / 200 skipped @ 657990caf**: **+10 passed, skipped unchanged**, accounted exactly by the new tests (torture 1 + epub 3 + appendix-letter parity 2 + docx CLI 1 + lst-sugar unit 3). One `LEAK`-but-passing test in the workspace run: `quarto-preview::join_frontend::editor_ui_boots_from_local_editor_index` — unrelated to P3 (no P3 code touches quarto-preview/join_frontend), noted for awareness.

## Details

**End-to-end verification (this repo's standing rule, not optional):** render the reduced `orange-book`-style fixture via `cargo run --bin q2 -- render <fixture> --to typst`, then actually open/inspect the compiled PDF (not just the intermediate `.typ` text) for at least the chapter-scoped figure numbering and the appendix lettering, and do the same for a real `--to epub` render (unzip, inspect XHTML). Record the exact invocation and what was inspected in this plan's checklist as each item completes, per the repo's end-to-end verification convention.
