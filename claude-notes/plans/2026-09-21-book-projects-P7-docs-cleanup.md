# Plan: Docs, templates, closeout (book-projects P7)

**Date:** 2026-09-21
**Epic:** [`2026-09-21-book-projects-epic.md`](2026-09-21-book-projects-epic.md)
**Design (authoritative):** [`../designs/book-projects-architecture.md`](../designs/book-projects-architecture.md) §11

## Overview

Close out the epic: user-facing docs, project-creation templates, and retiring the "not yet supported" diagnostics now that book projects are real. Mostly port/closeout work, no new architecture.

## Checklist

*(Reformatted into the "Tests first"/"Implementation" split every other phase plan uses, per a review pass's consistency finding — no scope change from the prior flat list, beyond the two items called out below as new.)*

### Tests first
- [x] Unit test: an extension `_extension.yml` with `contributes.project: {project: {type: book}}` now resolves successfully to a book-based custom project type (previously Q-16-7). — `base_type_book_resolves_successfully` (`crates/quarto-core/tests/integration/custom_project_type.rs`, rewritten from the old `base_type_book_is_a_q_16_7_error`).
- [x] Unit test: the same for `type: manuscript` still produces Q-16-7 unchanged. — `base_type_manuscript_is_still_a_q_16_7_error`, same file.
- [x] Unit test: `project: type: book` in `_quarto.yml` no longer produces the Q-5-18 warning; `type: manuscript` still does. — already existed and already passed: `book_project_type_does_not_warn` / `manuscript_project_type_warns_q_5_18` (`crates/quarto-core/tests/integration/project_type_parsing.rs`), landed in P1 when `project_kind_diagnostics` was narrowed to `ProjectKind::Manuscript` only. No new test needed; confirmed still green.

### Implementation
- [x] `docs/guides/projects/book.qmd` — usage-focused (per this repo's docs/ convention: document usage, not internals), covering `book.chapters`/`appendices`/`parts`, single-file vs. multi-file output selection, and the DOCX/PPTX/LaTeX scope-out (P3 for the docx/pptx diagnostic; LaTeX per design doc §11, never in scope at all). Document, as known v1 differences from Q1 rather than leaving them for users to discover, **all** of the epic's Known Limitations that are user-visible (list updated by a fifteenth-pass taxonomy review — this docs page is the only user-facing commitment to any of them, so an incomplete list here means real, silent-to-the-user gaps): (1) a Typst book now auto-resolves to the vendored `orange-book` extension when no explicit extension is configured, mirroring Q1's zero-config default (P2/design doc §5) — the remaining, narrower difference from Q1 worth documenting is that overriding to a *different* Typst book extension has no general auto-load mechanism behind it yet; (2) `q2 preview` (per P8, if landed by the time this page is written — otherwise describe the pre-P8 state and update once P8 ships) gets chapter-numbering/title decoration and approximate cross-chapter crossref resolution, but not a merged bibliography preview or single-file-merge preview — and note that a duplicate crossref id across chapters previews cleanly (non-fatal, first-in-book-order-wins) but produces a hard error on a real render; (3) per-chapter subtitle/author-affiliations/date/abstract front matter is not rendered (`bd-ock6cyiy`); (4) **numeric citation styles renumber independently per chapter in multi-file HTML books** — the limitation the epic explicitly prides itself on documenting "unlike Q1's silence" (P6's Decisions), so it must actually reach this page, not just an internal test; (5) no incremental rebuilds for books; (6) both single-file merge and multi-file Pass 2→Pass 3 hold every chapter's data in memory for one render, unverified at 50+ chapters; (7) `q2 preview`'s cross-chapter crossref sweep (P8) recomputes on every preview call for a book project, uncached, adding real per-edit latency on top of Pass 1's own (cached) sweep — unverified at large book sizes; (8) **added by a nineteenth-pass review, missing from this enumeration since this list was last updated in the eleventh pass, before the limitation existed (found in the eighteenth pass)**: Pass 3's resume/write loop is not atomic across chapters — a chapter can be written to disk embedding a resolved number/link into a sibling chapter before that sibling's own resume-and-write happens, so if the sibling's write later fails, the already-written chapter is left referencing content that was never produced, until the next full re-render. The overall render still reports failure (not silent), but the stale partial output on disk is a real, user-visible consequence worth documenting explicitly, matching the treatment every other numbered item on this list already gets.
- [x] Wire the new page into `docs/_quarto.yml`'s sidebar. — added right after `guides/projects/create.qmd`.
- [x] End-to-end verify: `cargo run --bin q2 -- render docs/guides/projects/book.qmd`, inspect rendered output, confirm sidebar link resolves. — rendered clean (one `Q-2-13` unclosed-emphasis error found and fixed: `**` immediately before a backtick doesn't open emphasis in Q2's markdown parser — two "Known limitations" bullets moved the bold span off the adjacent inline code); confirmed `book.html`'s sidebar entry is present and `class="... active"` on its own page.
- [x] Enable the "book" entry in `q2 create`/hub-client project templates, with a scaffold matching Q1's own default (`book.ts`'s `create()`): an unnumbered Preface (`index.qmd`), an `Introduction` chapter, a `Summary` chapter, and an unnumbered `References` chapter (no code, just the `{#refs}` div), plus `cover.png` and a starter bibliography. **One deliberate deviation from Q1, found by real end-to-end rendering, not assumed:** Q2's citeproc filter (`pampa::citeproc_filter::load_bibliography`) only parses CSL-JSON — there is no BibTeX parser — so the starter bibliography ships as `references.json` (CSL-JSON), not Q1's `references.bib`. Shipping the `.bib` file as originally planned silently failed every render (`Failed to parse bibliography: expected value at line 1 column 1`); caught by actually rendering the scaffold, per this epic's end-to-end-verification rule. Code: `crates/quarto-project-create/src/{choices,scaffold,templates,lib}.rs` + `resources/templates/book/*`. New regression test: `book_scaffold_produces_a_render_ready_file_set` (`lib.rs`). Real end-to-end: rendered the scaffold to HTML, Typst (PDF via `pdftotext`), and EPUB — chapter numbering, unnumbered Preface/References, and the merged bibliography (`(Knuth 1984)` + full CSL entry) all correct in all three formats.
- [x] Lift `Q-16-7` and `Q-5-18` for `Book` specifically:
  - `project_kind_diagnostics` (`crates/quarto-core/src/project/mod.rs`, ~line 357) was **already** narrowed to `ProjectKind::Manuscript` only — landed in P1, not this phase. Confirmed via `book_project_type_does_not_warn`/`manuscript_project_type_warns_q_5_18` (already green, see "Tests first" above).
  - The `contributes.project` extension-contribution check (same file, ~line 555): split `Ok(kind @ (ProjectKind::Book | ProjectKind::Manuscript))` into `Ok(kind @ ProjectKind::Manuscript)` only; `Book` now falls through to the `Ok(kind) => kind` success arm. Also updated the adjacent "not a built-in project type" error text (~line 574) to list `book` as a valid base alongside `default`/`website`.
  - **Rewrote** (not just wording-checked) `docs/errors/project/Q-5-18.qmd` and `docs/errors/extension/Q-16-7.qmd`: both now describe the warning/error as manuscript-only, with a forward pointer to the new book projects guide for the now-working case.
  - This did reduce to exactly the "delete the error arm plus tests" scope `bd-m1fzvpov` predicted.
- [x] **Before closing `bd-m1fzvpov`, confirm or file a follow-on strand for Manuscript's half.** Filed `bd-q0wjtmbu` ("Custom project types with a manuscript base"), `discovered-from` `bd-m1fzvpov`.
- [x] Close `bd-m1fzvpov` ("Custom project types with book/manuscript bases") — closed, reason recorded, Manuscript's half tracked separately at `bd-q0wjtmbu`.
- [x] File a new strand for `bd-bwwv` (book-style secondary navbar) as an explicit follow-on if it isn't already scoped clearly enough to stand alone. — re-read it: already adequately scoped as a standing strand (names the Q1 behavior, the required `ProjectType`-specific extension point, and the originating plan doc). No new strand filed; none needed.
- [x] Full workspace verification before any push: `cargo build --workspace`, `cargo nextest run --workspace` (report delta against the current live baseline, not a figure copied from an earlier phase), `cargo xtask verify` (full, since this epic touches `quarto-core`). — see gate record below.
- [x] Reconcile every phase plan's checklist against reality (re-read each, verify each item against what actually landed) before handing off to `finishing-a-development-branch`, per this repo's standing plan-closeout rule. — done via a dedicated sub-agent audit before starting P7 implementation (confirmed P0–P6 and P8 all fully checked off, gates recorded, no drift) plus this phase's own checklist above.

## Details

This phase should also be the point where the epic's own plan files (P0–P6, this one) get a final pass: correct any checkmarks that drifted from what was actually implemented, and note any discovered-but-deferred work as its own strand (per this repo's "beads vs. plans" rule — anything *outside* this epic's scope, not anything that was actually part of completing it).

### Gate record (2026-09-26)

Pre-implementation audit (dedicated sub-agent, read-only): P0, P1, P2, P3,
P4, P5, P6, and P8 all fully checked off in their own plan files, gates
recorded, git history confirms real implementation commits for each —
only P7 had zero implementation commits (planning-pass commits only).
No drift found; no corrections needed to earlier phases' checkmarks.

- `cargo clippy -p quarto-core -p quarto-project-create -p quarto --all-targets -- -D warnings`: clean.
- `cargo nextest run -p quarto-core -p quarto-project-create`: 5135/5135 passed, 31 skipped.
- Full `cargo build --workspace`: clean.
- Phase-boundary `cargo nextest run --workspace`: **14939/14939 passed, 200 skipped** — **+2** vs the live baseline (14937/200 @ HEAD `1da7a87d8`, measured directly via `git stash`/re-run, not copied from an earlier phase). Censused by test: `base_type_manuscript_is_still_a_q_16_7_error` (new, `quarto-core`) and `book_scaffold_produces_a_render_ready_file_set` (new, `quarto-project-create`); `base_type_book_is_a_q_16_7_error` was rewritten in place as `base_type_book_resolves_successfully` (net zero). One pre-existing test (`quarto::commands::create::interactive_tests::no_args_prompts_type_directory_title`) needed its hardcoded choice-label list updated (`["Default", "Website", "Blog"]` → `["Default", "Website", "Blog", "Book"]`) since `book` is no longer filtered out of the interactive prompt — not a new test, just an assertion that had to track the newly-implemented choice.
- `cargo xtask verify` (full, no skips — includes the wasm-quarto-hub-client build): **All verification steps passed!**
- Real end-to-end verification (not just tests): scaffolded a fresh book project via the actual `q2` binary and rendered it to HTML, Typst (PDF, text-extracted and read), and EPUB. All three formats show correct chapter numbering (Preface/References unnumbered, Introduction=1, Summary=2), and a correctly merged, deduplicated bibliography with the real Knuth 1984 citation and full reference entry. Also verified the `Q-5-33` diagnostic fires cleanly for `--to docx`, and confirmed live CLI output (`q2 create --list`, `q2 create --list --json`, and the JSON error path) to get `docs/guides/projects/create.qmd`'s examples byte-accurate rather than guessed.
