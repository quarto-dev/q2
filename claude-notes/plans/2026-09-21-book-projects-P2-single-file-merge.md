# Plan: Single-file chapter merge mechanism (book-projects P2)

**Date:** 2026-09-21 (revised twice after review passes; this revision folds in the already-vendored `book-*.lua` files)
**Epic:** [`2026-09-21-book-projects-epic.md`](2026-09-21-book-projects-epic.md)
**Design (authoritative):** [`../designs/book-projects-architecture.md`](../designs/book-projects-architecture.md) §5, §6, §8
**Q1 reference:** `book-render.ts`'s `mergeExecutedFiles`/`renderSingleFileBook` (concept only — Q1 concatenates markdown text with base64-encoded HTML-comment metadata; Q2 merges Pandoc AST directly). **Also**: the already-vendored `resources/pandoc-filters/filters/quarto-pre/book-numbering.lua`, `quarto-pre/book-links.lua`, `quarto-finalize/book-cleanup.lua`, `quarto-post/book.lua` — read these before writing any code in this plan, they do real work this plan depends on.

## Overview

The one genuinely new capability this epic needs for Typst/EPUB (and, validated but not implemented, LaTeX) book output: assemble several chapters into a single Pandoc document before the Crossref phase runs. Most of the hard part — chapter-scoped numbering, part/appendix structure, LaTeX's `\part{}`/`\appendix` sequence — turns out to already exist in vendored Lua from the pandoc-hybrid epic. This plan's actual job is: drive that Lua correctly (filter params, document attributes it expects) and add the small pieces of Rust it doesn't cover (the merge itself, cross-chapter link resolution, output-filename resolution).

This plan also builds the phase-bounded transform runner P5 (project-wide crossref registry) reuses — it's needed here first, at a different phase boundary (Normalization/Crossref) than P5's (Navigation/Finalization), but it's the same mechanism.

## Decisions

**Read `../designs/book-projects-architecture.md` §5 in full before implementing any of this section — it has the complete rationale for each vendored-Lua decision below.**

- Merge point is **after Normalization, before Crossref** — each chapter runs its own engine execution and AST-sugar transforms independently (unchanged), then bodies are concatenated as `Vec<Block>` (not markdown text).
- **New shared mechanism, redesigned by a sixteenth-pass "step back and consider alternatives" conversation — `execute_range` alone isn't the whole mechanism.** `TransformPipeline` gains a phase-range-bounded execute variant (`execute_range(&self, ast, ast_context, ctx, range: impl RangeBounds<TransformPhase>)`, using `TransformPhase`'s existing `Ord` derive) — an 8-line loop with all state living in `ast`/`ctx`, additive, not a redesign. But *invoking* it isn't free: `AstTransformsStage::run()` always calls `pipeline.execute(...)` today (`ast_transforms.rs:231`), for both the JIT-built and `with_pipeline`-supplied cases — there is no phase-bounded execution mode to reuse (an earlier draft overstated this as "an injection seam that already has a working consumer"; `build_analysis_pipeline` only ever customized pipeline *content*, never bounded *execution* of the real one). **Fix: make the range a first-class parameter of the stage itself** — `AstTransformsStage::for_range(range)`, whose `run()` always calls `execute_range(range)`; today's behavior becomes the default case where `range == ..`. Build this here.
- **Two stage-list shapes, not one, both built by this phase.** `build_html_pipeline_pause_stages(upper_bound)` — the prefix through a range-bounded `AstTransformsStage`, nothing after it — is what this phase's own per-chapter partial render uses (`..=Normalization`) and what P5's initial per-chapter render uses (`..=Navigation`). **`build_html_pipeline_finishing_stages(lower_bound)` is new, and this phase needs an instance of it too, not just P5**: the merged document's own run through "the unmodified single-document pipeline" starting from Crossref *is* this same primitive with `lower_bound = Crossref` — starts directly with a range-bounded `AstTransformsStage`, then the real, unchanged tail (`UserFiltersStage::post → ResourceReportStage → CodeHighlightStage → MathJsStage → RenderHtmlBodyStage → ApplyTemplateStage`, or the Pandoc-hybrid equivalent ending in `PandocWriteStage`/`TypstCompileStage`). Build it generically (parameterized on the lower bound); P5 reuses it with `Finalization..` instead of `Crossref..`, mirroring how `execute_range` itself is already assigned.
- **Part/appendix dividers are resolved, not TBD**: a `.quarto-book-part`-classed `Div` wrapping a `# Title` heading — exactly `book-render.ts`'s own `::: {.quarto-book-part}\n# Title\n:::`. The already-vendored `book-cleanup.lua` handles it from there: keeps it for LaTeX, unwraps it for Typst (its own comment claims book-numbering.lua already transformed the content to `#part[...]` — **that comment is wrong**, see below), deletes it entirely for every other format including EPUB (parts are print-only content; for EPUB/HTML they're sidebar-navigation-only, per §4/P1's sidebar translation).
- **Correction, found by reading `book-numbering.lua` directly rather than trusting `book-cleanup.lua`'s comment about it: `#part[...]` for Typst is not emitted by any vendored Quarto Lua at all.** `book-numbering.lua`'s `Header` handler has an explicit comment — "Typst part/appendix handling is delegated to book extensions (each Typst book package has different syntax for parts and appendices)" — and does no Typst-specific part/appendix work. The real `#part[...]` emission, and the **appendix-lettering mechanism** (a `#show: appendices.with(...)` rule that puts Typst's own numbering context into appendix mode), live in the Typst book extension's own filter (e.g. `orange-book.lua`). See design doc §5 for the full mechanism and why this matters for the decision below.
- **Two filter-param/attribute mechanisms activate the vendored Quarto Lua** — both required, neither optional, confirmed empirically that neither alone is sufficient:
  1. **`single-file-book: true`** filter param, set for every single-file-merge target (not just Typst) — gates `book-cleanup.lua`'s part handling, `book-numbering.lua`'s counter-reset generation, and (moot here, see below) `book-links.lua`.
  2. **`quarto-book-item-type`/`-number`/`-depth`/`-file`/`-appendix` Pandoc attributes**, stamped on each chapter's own top-level heading during merge (P2 already has this information from `BookRenderItem` — no separate tracking pass needed), consumed by a small, tracked patch to `book-numbering.lua`'s `Header` handler: read `el.attr.attributes["quarto-book-item-type"]` etc. directly instead of `currentFileMetadataState().file.bookItemType`. Document this patch the same way the existing `crossref-numbering` patch is tracked (a named, minimal diff against the pinned vendor tag) — patching vendored Lua is not a hard rule against here, and there's a draft upstream `quarto-cli` PR this could feed into.
- **Revised: emit the comment markers too, don't replace them.** The original plan here was "this replaces Q1's `<!-- quarto-file-metadata: ... -->` comment-marker mechanism" — patch-and-drop. That's wrong: the Typst book extension's own filter (`orange-book.lua`, used **unpatched** as this epic's test fixture — see below) learns `bookItemType`/`bookItemNumber`/`appendix` *exclusively* via `quarto.doc.file_metadata()`, which parses those exact comment markers and nothing else. Dropping them would silently break the extension's part/appendix/numbering logic with no error — a real, previously-missed failure mode (resolved with the user: emit both). **The merge step now stamps both forms** from the same already-known `BookRenderItem` fields: the Pandoc attributes above (for the tracked `book-numbering.lua` patch) *and* the original `<!-- quarto-file-metadata: base64(json) -->` markers in Q1's exact shape (`resourceDir`/`bookItemType`/`bookItemNumber`/`bookItemFile`/`bookItemDepth`, confirmed against `book-render.ts`'s `bookItemMetadata`) — no separate tracking pass, just two serializations of the same data.
- **`orange-book` is vendored locally, and auto-resolves as the default Typst book extension — resolved with the user (fifteenth pass), not left as "reference it explicitly from fixture config."** Q2 has no *general* bundled-extension infrastructure (see design doc §5/§11), but this epic mirrors Q1's one specific zero-config outcome it needs: a Typst book target with no explicit `format.typst` extension named resolves to the locally-vendored `orange-book`, mirroring Q1's own `render-contexts.ts` default (`readExtensionFormat`) for the `book` + `typst` pairing — hard-coded to this one pairing, not a general dispatch mechanism. Copy `_extensions/orange-book/` into a local resource directory per this repo's External Sources Policy (never referenced from `external-sources/` at runtime), alongside `resources/typst-packages/`. This is deliberately the *unpatched* extension — the point of the "emit both" decision above is that it should work without touching the extension's own code at all. A user who *does* explicitly name a different `format.typst` extension gets that one instead, unchanged; the default only fires when no extension is named at all.
- **`orange-book`'s vendored directory gets its own README** — a whole-system-composition review found this was the one vendored-resource directory in this epic with no source-tag/re-vendor documentation, unlike every sibling (`resources/scss/README.md`, `resources/typst-packages/README.md`, `resources/pandoc-filters/README.md`, `resources/revealjs/README.md`, `resources/js/README.md`). Write one in the same shape: which upstream tag/commit it's pinned to, and the procedure for re-vendoring when that pin moves.
- **Cross-chapter link resolution is Rust, not a Lua patch**: a new transform, modeled on `link_rewrite.rs`'s pattern but resolving to an in-document anchor instead of a separate output path, runs immediately after merge assembly (before Crossref). It has the complete picture at merge time (which chapter file maps to which real heading id). Once links are already `#<id>`-shaped, the already-vendored `book-links.lua` (gated on the same `single-file-book` param) sees nothing left to do and no-ops — no patch needed there. **Also**: don't attach a `ProjectIndex` to the merged document's `RenderContext`, so the pre-existing `LinkRewriteTransform` (Finalization, not in `PANDOC_TRANSFORM_EXCLUDED` — it runs for Pandoc-hybrid targets today) stays inert as a defensive backstop.
- **Scoped out, not silently dropped, but broader than "override": `quarto-post/book.lua`'s per-chapter `quarto-title-block` mechanism.** `resources/projects/book/pandoc/title-block.md` (the template it renders) covers subtitle, author affiliations, date, and abstract — driven by any chapter front matter setting those keys, not an author/title-override-specific case. Its guard means it's already inert if Q2 simply never emits that code-cell construct — no patch, no Rust work, just don't build the feature. This is missing functionality, not an intentional divergence; tracked as `bd-ock6cyiy`, not this phase's (or this epic's) job — document the real scope (subtitle/date/abstract, not just title/author) in the epic's Known Limitations, and see design doc §7 for the note asking P4 to leave its title-decoration insertion point extensible for that future work.
- Book-level title metadata (`book.title`/`author`/`date`/`abstract`/`doi` → merged document `meta`) is a direct port of `withBookTitleMetadata` — small, mechanical.
- `top-level-division: chapter` is the lever Typst and LaTeX share (confirmed by reading both `format-typst.ts` and `format-pdf.ts`'s `BookExtension`s, and empirically for LaTeX — see the appendix below). **It is not a general "this is a merged book" flag** — EPUB's chapter boundary is the separate `--split-level` mechanism (P3), and `single-file-book` (above) is a third, separate flag. Three distinct settings, three distinct jobs.
- **Format defaults specific to single-file-merge targets**, ported from `book.ts`'s `formatExtras`: `toc: false` for Typst specifically ("orange-book generates its own"), and `toc` already effectively `false` for EPUB (Q1's own default is `!isEpubOutput`). (`number-sections: true`/`crossref.chapters: true` are now P1's job, format-agnostically — this phase just inherits them; `crossref.chapters: true` matters concretely for EPUB's `sections.lua`-based numbering and is inert for Typst's native-counter path, but P2 doesn't need to set it itself.)
- **Output filename resolution**: port `bookOutputStem` — `book.output-file`, falling back to `book.title`, falling back to the project directory name, sanitized to a filesystem-safe name. Confirmed empirically: a real `quarto render . --to pdf` on a book titled "Numbering Probe" with no explicit `output-file` produced `Numbering-Probe.pdf`, not `index.pdf`.
- **Bibliography needs a real fix too — found by a review pass, not inherited for free from "citeproc just runs once over the merged whole."** That premise (design doc §2, before correction) was wrong: Q2's own citeproc filter (`apply_citeproc_filter`, inside `UserFiltersStage::pre()`) runs *before* `AstTransformsStage`/Normalization, for every format — meaning it would run independently per chapter, before this phase's merge step ever sees the chapters, producing N separate, non-deduplicated bibliographies rather than one unified one. **Fix, mechanism corrected by an implementability review**: `resolve_filters(...)` (`filter_resolve.rs:73`) is recomputed fresh from `doc.ast.meta["filters"]` *inside* `UserFiltersStage::run()` at stage-execution time — there is no seam for a caller to hand in an already-resolved, pre-edited filter list, so "exclude the `Citeproc` entry from each chapter's own resolved filter list" describes mutating an object that doesn't exist at any point this phase can reach. The actual mechanism: strip `"citeproc"` from each book-merge chapter's `meta["filters"]` array (metadata, not a resolved list) before that chapter's metadata reaches `UserFiltersStage` — i.e. a pre-render metadata mutation, checking both possible orderings (`.pre`/`.post`, since a user can order it into either) at the point that array is read, not a post-resolution filter-list edit. Run every other filter normally. After merge assembly, call `apply_citeproc_filter` (`crates/pampa/src/citeproc_filter.rs`, a standalone `Pandoc`+`ASTContext` function, no stage/`RenderContext` machinery needed) once, directly, on the merged document, before the single Crossref-Finalization pass. No new type, no dedup/aggregation logic, and — unlike an earlier draft of this fix — no dependency on P6's mechanism: placement into the references chapter is free via citeproc's own existing `{#refs}`-div-filling convention, which already lands wherever P1's `References`-kind chapter's own `{#refs}` div sits in the merged body. This achieves genuine Q1 parity (correct in-text citation numbers too, not just a merged list — see design doc §6 for why this is *better* than P6's necessarily-looser multi-file mechanism, not just different). Confirm at implementation time that no Normalization-phase transform assumes citeproc has already run on a chapter by the time it executes.
- The merged document then runs through the **unmodified** `feature/pandoc-writer-hybrid` single-document pipeline (Crossref phase, Finalization, `PandocWriteStage`) — no changes to that pipeline itself in this phase, except the one-time deferred citeproc call immediately after merge assembly, above. **This needs one new entry point that isn't itemized anywhere — found by an implementability review.** `render_qmd_to_pandoc`/`run_pipeline` both hardcode their *initial* `PipelineData` as `LoadedSource` built from raw QMD bytes (`pipeline.rs:800-803`) — there is no existing entry point that starts a stage list from an already-built `PipelineData::DocumentAst`, which is what the merged `Pandoc` already is. This is mechanically supported by the underlying machinery (`Pipeline::run` only checks `input.kind() == self.expected_input`; `AstTransformsStage`'s own `input_kind()`/`output_kind()` are already `DocumentAst`) but requires a genuinely new function — a `run_pipeline_from_ast` sibling, or a `run_pipeline` variant parameterized on the initial `PipelineData` — exactly as load-bearing as the per-chapter partial-render entry point above, and equally a checklist item, not a detail folded into "runs through the unmodified pipeline."
- **`run_with_book_support()`'s single-file-merge branch** (not a `ProjectType::render_documents` trait override — resolved by a seventeenth-pass research round, design doc §3): for a single-file-merge target format, this is where the merge logic lives — collect all chapters' post-Normalization ASTs, merge, hand off. (Multi-file HTML's branch, added in P4, is a different code path within the same concrete method, dispatched on target format.)

## Checklist

### Tests first
- [ ] Unit test: `execute_range` with an upper bound of `TransformPhase::Normalization` runs exactly the Normalization-phase transforms and none other, for a real pipeline built by `build_transform_pipeline`.
- [ ] Unit test: `execute_range` with a *lower*-bound-only range (e.g. `Finalization..`) runs exactly the Finalization-phase transforms and none other. P2 itself only needs the upper-bound form; this test exists purely so the lower-bound half of the shared contract is proven here, before P5 becomes the first real caller that depends on it.
- [ ] Regression test: `execute_range` with an unbounded range produces byte-identical output to today's `execute()` for an existing non-book fixture.
- [ ] **Unit test targeting the owned-struct/reconstruction mechanism itself, not just its end-to-end outcome** (gap found by a critical-review pass: this session's core architectural fix — never hold a constructed `RenderContext<'a>` across a pause, hold a plain owned per-chapter struct instead and reconstruct `RenderContext<'_>` fresh at each boundary call — had no test targeting the round-trip directly, only tests exercising it indirectly through the merge outcome). Build one chapter's owned struct, call the new partial-render entry point (`..=Normalization`), extract the mutated owned fields — **P2's actual list, corrected by a sixteenth-pass check of which fields this phase's target formats can even make live** (design doc §6 has the full reasoning): `artifacts`, `diagnostics`, `project_index`, `resource_resolver`, plus the seven static-passthrough fields (`ref_type_registry`, `options`, `includes`, `pipeline_profile`, `observer`, `user_grammar_provider`, `resource_report`) — assert each is present and correct on the extracted struct, independent of whether the merge step downstream does anything sensible with them. **Do not add `crossref_index`, `resource_copies`, `code_block_decorations`, `attribution_provider`, or `attribution_data` to this phase's struct or test** — each is dead for Typst/EPUB specifically (design doc §6: `crossref_index`'s phase hasn't run yet at this cut; `resource_copies`'s and `code_block_decorations`'s Normalization-phase writers/consumers are either HTML-gated or excluded via `PANDOC_TRANSFORM_EXCLUDED` for Pandoc-hybrid targets; `attribution_data`'s producer bails on format before ever running) — carrying them would be dead code, and P5's own plan (which does need all five) is where the corresponding test belongs.
- [ ] Unit test: merging 3 chapters' `Vec<Block>` bodies (each with a distinct H1) produces one document with 3 H1s in book order, each stamped with the correct `quarto-book-item-*` attributes *and* carrying the correctly-shaped `<!-- quarto-file-metadata: base64(json) -->` markers (decode and compare against Q1's exact `resourceDir`/`bookItemType`/`bookItemNumber`/`bookItemFile`/`bookItemDepth` shape) — no cross-talk between chapters' engine-execution state.
- [ ] Unit test: a `part:` entry between two chapter groups produces the `.quarto-book-part`-classed `Div` divider, positioned correctly relative to its chapters.
- [ ] Unit test: `withBookTitleMetadata`-equivalent sets the merged document's title/author/date from `book.*` config, not from any individual chapter's own front matter.
- [ ] **Integration test against the real, unmodified (except the tracked patch) vendored Lua**: a 3-chapter book with a figure in each chapter, merged with `single-file-book: true` and the heading attributes set, run through the *existing* Typst pipeline, produces one document where each chapter's figure numbers reset correctly (`Figure 1.1`, `Figure 2.1`) via `book-numbering.lua`'s dynamically-generated show-rule — not asserted from reading the Lua, actually compiled and inspected. This is the test that would have caught the "top-level-division alone is not sufficient" finding if it had existed earlier; keep it as a permanent regression guard.
- [ ] Integration test: the same merge *without* `single-file-book` set produces continuous, unreset numbering — a negative control proving the two mechanisms (`top-level-division`, `single-file-book`) are genuinely independent, not redundant.
- [ ] Integration test: an explicit cross-chapter link (`[see chapter 1](chapter1.qmd#sec-foo)`) resolves to the correct in-document anchor in the compiled output, via the new Rust transform — and confirm `book-links.lua` had nothing left to do (e.g. via a debug/trace assertion, or by testing that the same fixture without the Rust transform produces a broken/unresolved link, proving the Rust step is load-bearing, not redundant with the Lua).
- [ ] Integration test: an unnumbered chapter (`.unnumbered` on its H1), merged into a single-file document, contributes no chapter boundary to the compiler's native counter — its own floats number as part of whichever chapter precedes/follows it in the merged document's continuous structure. Do not claim this matches "Q1's observed behavior" until it has actually been observed for the merge-mode case specifically (P4's appendix observed this for multi-file mode only).
- [ ] Integration test: a chapter references an *earlier* chapter's labeled theorem after merge+compile — the reference must show the theorem's original chapter-scoped number. `orange-book`'s own test suite guards this exact case with "Critical" severity (a resolution-timing bug class in the Typst theorem package it uses); Q2 inherits this risk by construction, so needs the fixture, not a code fix.
- [ ] Integration test: a document using a custom `crossref.custom` kind (mirroring `orange-book`'s "Dinosaur") gets the same chapter-scoped, compile-time-resolved numbering as a built-in figure — this exercises `book-numbering.lua`'s dynamic `crossref.categories.all` walk, which is specifically what makes it correct for custom kinds where `orange-book`'s own bundled template alone is not.
- [ ] **Integration test against the real, *unpatched* `orange-book` extension** (vendored as a local fixture, not this epic's own patched Lua): a merged book with a `part:` divider and an appendix chapter, rendered through `orange-book`, produces a real `#part[...]` block and correct `Theorem A.1`-style appendix numbering in the compiled Typst output — with the extension's own filter untouched. This is the test that proves the comment-marker emission (not the Pandoc-attribute patch) is what makes an unpatched extension work; it would have caught the "patch-and-drop breaks orange-book" finding if it had existed earlier.
- [ ] **Integration test, resolved with the user (fifteenth pass): a book project with `--to typst` and *no* `format.typst` extension configured at all** — no `_extension.yml`, no explicit extension key anywhere in the fixture's `_quarto.yml` — resolves to the vendored `orange-book` automatically and renders/compiles successfully, matching Q1's own zero-config default. This is the test that makes the auto-default decision (see this plan's Decisions above, and design doc §5) a proven behavior rather than an assertion.
- [ ] **Documenting test, mirroring P6's numeric-citation-style pin**: a chapter's front matter sets `subtitle`/`date`/`abstract`; the merged, compiled output shows none of it. Pins the current (`bd-ock6cyiy`-tracked) silent-drop behavior explicitly, so a future change to it — fix or regression — is a deliberate, visible diff rather than silent drift either way.
- [ ] **Integration test, added by a review pass that found bibliography was not, in fact, automatic**: two chapters citing the same source, merged via this phase's mechanism with a numeric CSL style, produce book-wide-correct, non-duplicated *in-text* citation numbers (not just a deduplicated bibliography list) — the test that proves genuine Q1 parity for single-file merge, unlike P6's necessarily-looser multi-file mechanism.
- [ ] Integration test: two references sharing the same author+year across different chapters get correct year-suffix disambiguation in the merged single-file output (the deferred, once-only citeproc call gets this for free from `apply_citeproc_filter`'s normal document-wide behavior — assert it, don't assume it).
- [ ] Regression test/audit: confirm no Normalization-phase transform assumes a chapter's citeproc pass has already run by the time it executes (since this phase now defers it) — or add a targeted fix if one does.
- [ ] Integration test, cross-artifact, **data-level only — the compiled-PDF half of this assertion belongs to P3, not here** (ownership clarified by a review pass: this plan's own Details section says "P3 is the phase that wires this through real `--to typst`/`--to epub` invocations end-to-end and verifies the compiled output," so a test in *this* plan that requires inspecting a compiled PDF was mis-scoped). What P2 tests directly: the merge function preserves `BookRenderItem`'s appendix order exactly — the same order P1 computed for the sidebar — in the merged document's heading order, with no reordering/dropping/duplication. P3's checklist has the corresponding compiled-artifact-level assertion (the sidebar's appendix letter for a chapter matches the Typst extension's compiled letter for that same chapter) — together the two tests are the full regression guard on the order-preservation invariant (design doc §4), split at the boundary each phase actually owns.

### Implementation
- [ ] `TransformPipeline::execute_range` (or equivalent), reusing the existing loop with a phase-range filter, supporting both upper-bound-only and lower-bound-only ranges.
- [ ] **New per-chapter partial-render entry point — real new plumbing, with a working precedent to copy, not a design unknown.** `render_document_to_file` is the only production per-document path today, and it doesn't call `TransformPipeline::execute` itself — it delegates to `render_qmd_to_html`/`render_qmd_to_pandoc` (`pipeline.rs:951`/`400`), which each build a full ~19–25-stage list and run it via `Pipeline::run` (`stage/pipeline.rs:140`, a flat loop with no stopping point) ending in output-writing stages (`ApplyTemplateStage`/`PandocWriteStage`/etc.) this phase doesn't want for a paused chapter. Traced end to end (see design doc §6 for the full call chain, file:line references, and the resolved design — this checklist item summarizes it): this needs three pieces, not one, all following the existing precedent of `build_analysis_pipeline`/`build_analysis_transform_pipeline` (`pipeline.rs:687-743`), which already stops the pipeline early (Normalization+Crossref only) for an unrelated existing feature by handing `AstTransformsStage` a custom, pre-bounded `TransformPipeline` via `AstTransformsStage::with_pipeline(...)` (`ast_transforms.rs:77`) instead of letting it JIT-build the full one:
  1. `TransformPipeline::execute_range` itself (this checklist's item 1) — a one-line execution-time filter over the existing transform list using `TransformPhase`'s `Ord` derive, no new pipeline-construction machinery needed.
  2. A new short stage-list constructor: the prefix of `build_html_pipeline_stages_with_options` through `AstTransformsStage` (wired to call `execute_range` via a phase bound instead of `execute`), stopping before `UserFiltersStage::post`/`ResourceReportStage`/`CodeHighlightStage`/`MathJsStage`/`RenderHtmlBodyStage`/`ApplyTemplateStage` (or the Pandoc-leg equivalents). This duplicates part of the stage-list prefix, the same deliberate way `build_analysis_pipeline` already does — not new architectural surface.
  3. A thin new `render_qmd_to_*` sibling (mirroring `render_qmd_to_html`/`render_qmd_to_pandoc`'s own shape) that builds that short stage list and calls the existing `run_pipeline`, taking `ctx: &mut RenderContext<'_>` exactly like its siblings — meaning **this function does not own or construct `RenderContext`; the caller does**, which is what resolves the lifetime question below.

  **This also resolves the `RenderContext<'a>` lifetime worry, more simply than previously assumed**: since the new entry point takes `ctx: &mut RenderContext<'_>` rather than returning an owned one, nothing needs to hold a *constructed* `RenderContext<'a>` across a pause at all. This phase instead defines a plain owned struct per chapter (`doc_info: DocumentInfo`, `binaries: BinaryDependencies`, plus copies of `RenderContext`'s owned fields — **P2's own short list, not the full audited superset**: `artifacts`, `diagnostics`, `project_index`, `resource_resolver`, plus the seven static-passthrough fields — see design doc §6 for why `crossref_index`/`resource_copies`/`code_block_decorations`/`attribution_provider`/`attribution_data` are all genuinely dead for this phase's target formats specifically, not just omittable for convenience) and reconstructs a fresh, short-lived `RenderContext<'_>` borrowing from that struct's own fields immediately before each call into the new entry point, copying mutated owned fields back out afterward. No self-referential struct, no unsafe — this is the same shape `render_document_to_file` already uses locally (owns `doc_info`/`binaries`, borrows into `RenderContext` locally), just with the owning scope moved up to this phase's per-chapter merge loop. **P5's version of this struct is the full list, not this one** — see P5's plan.

  **This phase's own use of it is simpler than P5's**: P2 merges every chapter into one new document, so it only needs to take one chapter's `Pandoc` body at a time and can drop that chapter's owned struct immediately — no need to hold N chapters' state simultaneously here (that's P5's harder requirement, see its own plan; don't over-build P2's call pattern to match it). **Make the upper bound a parameter, not hardcoded to `Normalization`** — P2 calls the new entry point with `..=Normalization` and drops the struct per chapter; P5 needs the *same* entry point called with `..=Navigation`, its per-chapter struct *held* across all chapters simultaneously in a book-scoped `Vec`, since P4/P5 chapters run Navigation per-chapter and P5 must resume each chapter's own Finalization independently (N output files, not one merge) via a second call with `Finalization..`. If P5 has to build a near-duplicate entry point because this one is Normalization-specific or drop-only, that's the mistake this note exists to prevent — get the generalization right here so P5 actually reuses it.
- [ ] Merge function: `Vec<(BookRenderItem, Pandoc)>` → one merged `Pandoc`, inserting `.quarto-book-part` dividers, stamping `quarto-book-item-*` attributes *and* emitting the `<!-- quarto-file-metadata: base64(json) -->` comment markers (Q1's exact shape) on the same headings, and book-level metadata. **Found by an eighteenth-pass review: this merges `Pandoc` bodies only — the merged document's fresh `RenderContext` also needs an explicit construction contract, not left implicit.** Concatenate `diagnostics` and merge (union) `artifacts` across all N chapters' extracted structs into the merged context — otherwise a diagnostic or artifact registered during any chapter's own Normalization pass is silently lost once that chapter's owned struct is dropped. `project_index`/`resource_resolver` and the seven static-passthrough fields (`ref_type_registry`, `options`, `includes`, `pipeline_profile`, `observer`, `user_grammar_provider`, `resource_report`) are invariant across chapters (project-level, never chapter-mutated) — source them from any one chapter's copy, or compute once at book level before the per-chapter extraction loop starts, rather than leaving the choice to whichever implementer gets here first.
- [ ] `AstTransformsStage::for_range(range)` — the range becomes a first-class constructor parameter of the stage itself (default/production case is `range == ..`), with `run()` always calling `execute_range(range)`. This is what makes `execute_range` actually invocable; found missing by an implementability review (the stage unconditionally calls `execute` today).
- [ ] `build_html_pipeline_pause_stages(upper_bound)` — the prefix constructor, generalized to take the phase bound as a parameter (P2 calls it with `..=Normalization`; P5 reuses it with `..=Navigation`).
- [ ] `build_html_pipeline_finishing_stages(lower_bound)` + `run_pipeline_from_ast` (a `run_pipeline` sibling starting from an already-built `PipelineData::DocumentAst` instead of raw bytes) — **general mechanism, built here because this phase needs its own instance (`Crossref..`, feeding the merged `Pandoc` into the unmodified single-document pipeline) before P5 does (`Finalization..`, for its resume)** — found missing by an implementability review; every existing entry point hardcoded raw-QMD-bytes as the initial input. `run_pipeline_from_ast` needs its own complete `RenderContext↔StageContext` bridge (see this phase's owned-field list above for what it must carry for *this* phase's calls; P5's plan has its own, longer list for its calls to the same function).
- [ ] `single-file-book` filter param wiring for merged documents.
- [ ] The tracked patch to `book-numbering.lua`'s `Header` handler (attribute reads instead of `currentFileMetadataState()`) — this patch is additive to the comment-marker emission above, not a replacement for it. Document it in `resources/pandoc-filters/README.md` alongside the existing `crossref-numbering` patch note.
- [ ] Vendor `orange-book`'s `_extensions/orange-book/` locally (per the External Sources Policy), alongside `resources/typst-packages/`.
- [ ] `orange-book`'s README: source tag/commit + re-vendor procedure, matching every sibling vendored-resource directory's existing convention (`resources/scss/README.md`, `resources/typst-packages/README.md`, `resources/pandoc-filters/README.md`, `resources/revealjs/README.md`, `resources/js/README.md`).
- [ ] **Default-extension resolution for Typst book targets** — resolved with the user (fifteenth pass), a real implementation item, not a fixture-only convenience: when a book target's format is Typst and no `format.typst` extension is explicitly named, resolve to the locally-vendored `orange-book` automatically (mirroring Q1's `render-contexts.ts`'s `readExtensionFormat` outcome for exactly this pairing — hard-coded to `book` + `typst` → `orange-book`, not a general extension-dispatch mechanism). This is production behavior, not test-fixture wiring — a real user's book project gets this default with no config, matching Q1. **Concrete injection point, found by an implementability review (neither the design doc nor this plan had cited one)**: `parse_format_descriptor(format: &str) -> FormatDescriptor { extension_name: Option<String>, base_format: String }` (`extension/discover.rs:252`) already distinguishes "no explicit extension" (`extension_name: None`) from an extension-qualified format string, and is already called from `MetadataMergeStage` (`metadata_merge.rs:94`) — a point where `ProjectKind::Book` is already known. Rewrite the target-format string to name `orange-book` (or set `extension_name` directly) there, rather than inventing a new seam.
- [ ] New Rust cross-chapter link-resolve transform, running post-merge, pre-Crossref.
- [ ] Confirm the merged document's `RenderContext` is built without a `ProjectIndex`.
- [ ] `top-level-division: chapter` plumbing into the Pandoc-hybrid invocation builder, gated on target format being Typst or LaTeX and the document being a book merge.
- [ ] `toc: false` for Typst applied to the merged document's metadata (the format-agnostic `number-sections`/`crossref.chapters` defaults are P1's job — this phase inherits them, nothing to implement here).
- [ ] Output filename resolution (`bookOutputStem` port).
- [ ] Strip `"citeproc"` from each book-merge chapter's `meta["filters"]` array (both `.pre` and `.post` orderings) before that chapter's metadata reaches `UserFiltersStage` — **not** a post-resolution filter-list edit; `resolve_filters` recomputes fresh from metadata inside `UserFiltersStage::run()`, so there's no reachable resolved-list object to mutate (corrected by an implementability review). Call `apply_citeproc_filter` once on the assembled merged document, before the single Crossref-Finalization pass — added by a review pass, see the Decision above.
- [ ] `run_with_book_support()`'s single-file-merge branch.
- [ ] `cargo clippy -p quarto-core --all-targets -- -D warnings`; `cargo nextest run -p quarto-core`; phase-boundary `cargo nextest run --workspace` with delta reported.

## Details

This phase deliberately does not touch Typst-specific or EPUB-specific *pipeline* code beyond the filter-param/attribute wiring above — it produces a merged `Pandoc` document, drives the already-vendored Lua correctly, and hands off to the unmodified single-document pipeline. P3 is the phase that wires this through real `--to typst`/`--to epub` invocations end-to-end and verifies the compiled output.

## Appendix: LaTeX empirical confirmation (upgrading a design-doc claim from "read the source" to "verified")

Built a 3-chapter + 1-appendix book fixture and rendered it with the real `quarto` binary, `--to pdf` with `keep-tex: true`, and inspected the intermediate `.tex` directly.

Observed:
```
\chapter*{Preface}\label{preface}
\chapter{Chapter One}\label{sec-ch1}
\section{A subsection}\label{sec-sub1}
\chapter{Chapter Two}\label{sec-ch2}
\cleardoublepage
\phantomsection
\addcontentsline{toc}{part}{Appendices}
\appendix
\chapter{Appendix Stuff}\label{sec-apx1}
```
and, for cross-references: `See Chapter~\ref{sec-ch1} and Figure~\ref{fig-a} from chapter one.` — a genuine `\ref{}`, resolved by LaTeX at compile time. The full `\cleardoublepage`/`\phantomsection`/`\addcontentsline`/`\appendix` sequence before the appendix chapter matches `book-numbering.lua`'s `Header` handler exactly (confirmed after re-checking with fuller context — an initial narrower grep had only shown the trailing `\appendix` line and looked like a discrepancy; it wasn't). This is structurally identical to Typst's `#ref()`/native-counter behavior, just via a different compiler's own native mechanism and different vendored-Lua code path. LaTeX book output remains unimplemented this epic, but the abstraction P2 is built around is now validated against two independent compilers. (For completeness, if LaTeX book output is ever picked up: `book.ts`'s `formatExtras` also sets `documentclass: scrreprt` and `papersize: letter` alongside `top-level-division: chapter` for LaTeX specifically.)

## Appendix: multi-chapter Typst validation (the empirical finding that corrected this plan)

Built a minimal 2-H1 Typst document (not a full book merge — just enough to isolate the mechanism) and rendered it with the real, already-built `q2` binary on this branch:

- With neither `top-level-division: chapter` nor `single-file-book` set: figures numbered continuously (`Figure 1`, `Figure 2`) — the baseline.
- With `top-level-division: chapter` set alone: **identical result** — still `Figure 1`, `Figure 2`, no chapter-scoping at all. This is the finding that overturned an earlier draft of this design, which assumed the counter reset was baked into the static vendored template and needed nothing from Q2 beyond real chapter boundaries.
- Chapter-scoped reset requires `book-numbering.lua`'s dynamically-generated show-rule, which only fires under `single-file-book: true` — not yet re-tested end-to-end with that param set (doing so meaningfully requires this plan's own merge/attribute machinery to exist first), but the mechanism is now fully understood from reading the Lua directly, not inferred.
