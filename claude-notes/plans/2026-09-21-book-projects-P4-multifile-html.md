# Plan: Multi-file HTML book mode, local numbering (book-projects P4)

**Date:** 2026-09-21 (revised after a critical review pass)
**Epic:** [`2026-09-21-book-projects-epic.md`](2026-09-21-book-projects-epic.md)
**Design (authoritative):** [`../designs/book-projects-architecture.md`](../designs/book-projects-architecture.md) §7
**Q1 reference:** `book-chapters.ts`'s `withChapterMetadata`/`formatChapterTitle`/`chapterInfoForInput`, `resources/filters/crossref/sections.lua`, `book.ts`'s `bookScssBundle`/`bookHtmlPostprocessor`, `book-render.ts`'s cover-image markdown prepend

## Overview

Each chapter renders independently — exactly today's Pass 2, unchanged — but needs one externally-injected fact before its own Crossref-phase transform runs: its book chapter number (and appendix-letter flag). This phase ships a fully usable multi-file HTML book *before* cross-chapter reference resolution exists (P5): a same-chapter `@ref` resolves correctly; a cross-chapter `@ref` shows a visible, honest "unresolved" placeholder rather than silently wrong output.

This phase also owns the two book-specific features that are genuinely tied to HTML rendering (not config-level, hence not P1): the book stylesheet and the cover image.

## Decisions (see design doc §7 and the appendix below for the experiments that ground these)

- Keep an explicit `chapter_number: u32` / `is_appendix: bool` fact (computed once in `pre_render` from P1's `BookRenderItem` list) as the single source of truth for "which chapter is this" — used by title decoration, sidebar, and P5's future registry key.
- This fact has **two** numbering-related consumers, not one (corrected on review — an earlier draft said "only consumer"): seeding `CrossrefIndex.sections = vec![chapter_number - 1]` (P0's seed capability) before that chapter's `CrossrefIndexTransform` walk begins, and P0's `@sec-`/section-number *presentation* code, which needs `is_appendix` on the `RenderContext` to letter-format (not numeral-format) the resulting number and prefix "Appendix" instead of "Chapter" (see design doc §7 and P0's plan — `sections.lua` does the equivalent). The counter itself stays appendix-agnostic; only presentation branches on `is_appendix`. No parallel counter, no changes to `advance_sections` itself. An unnumbered chapter (`chapter_number: None`) gets no seed at all — its own local numbering starts fresh and unscoped, exactly as observed (see appendix).
- **How the fact actually reaches `RenderContext` — traced against real code, not assumed (a critical-review pass caught that "threaded into each chapter's `RenderContext`" had no implementation path as originally written).** Neither `RenderToFileOptions` nor `Pass2Renderer::render_batch*` has any per-document seed channel today, and `RenderContext` is built entirely internally inside `render_document_to_file` with no caller hook. **P0 builds the `RenderContext`-level half of the fix already** — `ChapterSeed{chapter_number, is_appendix}`, the `RenderContext.chapter_seed` field, and the `RenderContext::with_chapter_seed(...)` builder (following the existing `with_project_index`/`with_resource_resolver`/`with_options` pattern) — since P0 needs that plumbing self-contained for its own tests and runs before this phase. **This phase's job is the other half: getting a *real* per-chapter value into that already-built field for an actual multi-chapter render.** `render_document_to_file`/`RenderContext::new` are low-risk to extend for this (only two production call sites for `RenderContext::new`, no stray struct-literal construction elsewhere in the crate): add a new `chapter_seed: Option<ChapterSeed>` field on `RenderToFileOptions` (already derives `Debug, Clone, Default`, so no other caller is affected), applied via `.with_chapter_seed(...)` right after `RenderContext::new(...)` in `render_document_to_file`, before the pipeline runs.

  **Revised after a review pass (tenth pass): don't touch the `Pass2Renderer` trait signature at all.** The original plan here added a `HashMap<PathBuf, ChapterSeed>` parameter directly to `Pass2Renderer::render_batch`/`render_batch_serial`/`render_batch_parallel` — but `render_batch` is a *trait* method (`crates/quarto-core/src/project/pass2_renderer.rs:126`), implemented by `RenderToHtmlRenderer` and `RenderToPreviewAstRenderer` (the WASM/preview renderers) as well as `RenderToFileRenderer` (the only one that ever renders a book chapter). Widening the trait signature would force every current and future `Pass2Renderer` implementor to accept and ignore a parameter meaningless to it. **Fix: confine the seed map to `RenderToFileRenderer` itself**, via a new field (`chapter_seeds: HashMap<PathBuf, ChapterSeed>`, default empty) and a matching `RenderToFileRenderer::with_chapter_seeds(mut self, seeds: HashMap<PathBuf, ChapterSeed>) -> Self` builder — the same idiom `RenderContext`'s own builders use, and consistent with how `format_override` already flows into this same struct from `ProjectPipeline::with_format_override` (`pass2_renderer.rs:239-245`). No trait method gains a new parameter; `Pass2Renderer::render_batch`'s signature is untouched, so `RenderToHtmlRenderer`/`RenderToPreviewAstRenderer` need no changes at all.

  **One real mechanical wrinkle this surfaces, not avoided by either approach: `RenderToFileRenderer.options: &'a RenderToFileOptions` is one shared borrowed reference used for every document in a batch call** (`pass2_renderer.rs:239`, `render_batch_serial`/`render_batch_parallel` both take `options: &RenderToFileOptions` and forward the same reference to every `render_document_to_file` call in the loop, `pass2_renderer.rs:374-414`, `432-483`). A per-document seed can't be expressed by mutating that shared reference. Since `RenderToFileOptions` is `Clone` (already derived), the fix is: **the lookup itself has to live inside `render_batch_serial`/`render_batch_parallel`, not `RenderToFileRenderer::render`/`render_batch`** (corrected by an implementability review — those two methods just dispatch to the free functions, which own the actual per-doc loop; `render`/`render_batch` never see individual documents). Since those free functions' public contract can grow one additional plain parameter (they're private to this module and not part of the `Pass2Renderer` trait), thread `self.chapter_seeds` into them; inside the per-doc loop, look up `chapter_seeds.get(&doc.input)`; if present, clone `options`, call `.with_chapter_seed(...)` on the clone, and pass that owned value to `render_document_to_file` for that one document; if absent, pass `options` through unchanged (no clone in the common non-book path). Keyed by each chapter's own input path, not position — `render_batch_parallel`'s `par_iter` doesn't preserve a stable index-to-doc mapping worth relying on.

  **This still deliberately keeps using `Pass2Renderer::render_batch*` rather than a bespoke per-chapter loop** — `render_batch_parallel` is real, already-tuned machinery (a dedicated rayon thread pool, `catch_unwind` panic safety, `fail_fast` short-circuiting, ordered artifact-store merging, perf instrumentation), not a thin wrapper worth reimplementing per project type. Book chapters get the same parallel rendering every other project type gets, for free — not a Known Limitation, a non-issue.
- Chapter title decoration ports `withChapterMetadata`/`formatChapterTitle` (chapter number prepended to the page's own H1/title metadata, "Appendix A — Title" formatting, `.unnumbered`/`.unlisted` handling) as idiomatic Rust. **This is distinct from, and does not cover, sidebar nav-item text decoration** (`numberChapterHtmlNav`'s equivalent) — that's P1's job, baked into the sidebar translation at config time rather than here at render time (see P1's plan for why). **Leave this insertion point extensible**: `bd-ock6cyiy` (per-chapter subtitle/author-affiliations/date/abstract, tracked separately, not this phase's job) has the same natural seam — insert the title-decoration content as one block in what could later be a short, ordered sequence of decoration blocks near the H1, not a single hardcoded block that a future change would have to restructure.
- **`number-sections: true`/`crossref.chapters: true` are P1's job now**, applied uniformly regardless of format — this phase inherits them rather than setting them itself (previously mis-scoped to P2 only; see P1's plan).
- **`book.scss`**: port `bookScssBundle` directly — an SCSS bundle Q1 injects for HTML output, gated the same way Q1 gates it (Bootstrap-based HTML themes).
- **Cover image**: Q1's mechanism is two parts — (1) prepend a markdown image to the index/Preface chapter's content (`book-render.ts`'s `onRender`), and (2) a DOM postprocessor (`bookHtmlPostprocessor`) that moves the resulting `<img>` from before the first `<section>` wrapper to inside it, because Pandoc's HTML writer wraps heading+content in `<section>` and the image — prepended to raw markdown before any heading — lands outside that wrapper. **Do not port part (2).** Per this repo's no-DOM-postprocessor policy, insert the cover-image `Block` in the *correct* position in the AST from the start (inside the section, immediately after the opening heading) rather than porting a repositioning step. **Confirmed against the real writer (design doc §7): the writer itself does no section logic — a separate pre-write pass, `SectionizeTransform` (Normalization phase, `pipeline.rs:1392`), wraps headings into `Div{class="section level<N>"}` by heading level before the HTML writer ever runs.** Inserting the cover-image `Block` right after the index chapter's `Header` works *only if done before `SectionizeTransform` runs* — do this insertion as an early Normalization-phase step (the same pass that already knows, from P1's chapter model, "this is the index/Preface chapter"), not a later pass; a later insertion point would have to splice into the already-built section `Div`'s content instead of the flat block list.
- A cross-chapter `@ref` uses the crossref system's *already-existing* unresolved-reference mechanism (`CrossrefResolveTransform` already emits an unresolved `CrossrefResolvedRef` node, specifically so a renderer can show a placeholder instead of erroring) — no new placeholder shape.
- **`run_with_book_support()`'s multi-file branch** (not a `ProjectType::render_documents` trait override — resolved by a seventeenth-pass research round, design doc §3) (dispatched on target format being HTML-based) drives chapters through the ordinary per-document `RenderToFileRenderer`/`Pass2Renderer` path — unmodified except for the `with_chapter_seeds(...)` call described above, which is how the chapter-number/appendix fact actually reaches each chapter's `RenderContext`.
- **Out of scope for this phase, and for the epic**: `q2 preview` never calls `run_with_book_support()`'s book orchestration at all — it renders a single active chapter through a different per-document code path (`RenderToHtmlRenderer`/`RenderToPreviewAstRenderer`). A previewed chapter therefore gets none of this phase's chapter-number seeding or title decoration; it renders exactly as if it were a lone, non-book document. Documented as a deliberate epic-level Known Limitation, not something this phase needs to address.

## Checklist

### Tests first
- [x] Unit test: seeding `CrossrefIndex.sections = vec![chapter_number - 1]` for chapter 2 makes its own H1 become section `2`, and a figure under it becomes "Figure 2.1". **The figure half of this test required a real fix, found by an implementability review and resolved by a follow-up research round — the underlying data (`order.section`, seeded via `index_custom_target` for every crossref target, not just headers) was already correct; the bug was `crossref_render.rs`'s display functions reading `order.order` alone.** See the Implementation item below and design doc §7 for the resolved fix (read `ctx.chapter_seed.chapter_number` directly, shared formatting helper).
  - `crates/quarto-core/src/transforms/crossref_render.rs::chapter_two_seed_shows_h1_two_and_figure_two_one` (via P0's `run_full_opts` harness). Verified RED at the figure-caption assertion — got `"Figure\u{a0}1: "`, want `"Figure\u{a0}2.1: "`; the H1 `assert_number_span(.., "2")` assertion *passed*, confirming P0's seeding half already works and the failure is precisely the display half this phase fixes.
- [x] Unit test: appendix chapters get a letter-scoped number derived from their position in the appendix sequence (P1's `BookRenderItem` numbering), independent of the main chapter count.
  - `crates/quarto-core/src/transforms/crossref_render.rs::appendix_seed_numbers_floats_letter_scoped` — appendix-local number 3 (third appendix) with zero main chapters: caption `"Figure\u{a0}C.1: "` and in-text ref `"Figure\u{a0}C.1"`. Verified RED — got `"Figure\u{a0}1: "` / `"Figure\u{a0}1"`.
- [x] Integration test: a 3-chapter + 1-appendix book, multi-file HTML, renders N separate files with correct per-chapter-local numbers, matching the `scratchpad/minibook` experiment's observed output exactly (chapter 1 → `1.1`, chapter 2 → `2.1`, appendix → `A.1`).
  - `tests/integration/book_multifile_html.rs::three_chapter_book_renders_separate_files_with_chapter_local_numbers`. RED (flat numbers), then GREEN after the ctx-bridge fix below. Also verified end-to-end through the real CLI (`cargo run --bin q2 -- render .tmp-p4-probe --to html`): ch1 `header-section-number">1<` + `Figure 1.1`, ch2 `">2<` + `Figure 2.1`, app-a `">A<` + `Figure A.1` with the `Appendix <span…>A</span> —` H1 shape.
  - **Fixture-shape finding (discovered by this test, applies generally, not a P4 regression):** a fenced div + `#| fig-cap:` line does **not** attach a caption in q2's render path — the `#|` line leaks as literal paragraph text and the float renders `quarto-uncaptioned` with a flat label. Verified identical in a standalone non-book render (so not book-path-specific). The working shape is the **code-block shorthand** (```{.python}` + `#| label:` + `#| fig-cap:` inside the fence, unexecuted `{.python}` — the executed `{python}` form demands a jupyter kernel). The test fixture uses the shorthand; assertions unchanged.
- [x] Integration test: an unnumbered chapter inserted between two numbered chapters (a) consumes no chapter-number slot — the following chapter's numbers are unaffected (verified: chapter 2's figure stayed `Figure 2.1` with an unnumbered chapter inserted before it, not `3.1`), and (b) its own figure numbers as a plain, flat, unscoped `Figure 1` — not `Figure 0.1`, not chapter-scoped at all. Both halves of this claim were directly observed against a real render (see appendix); do not weaken this to "matching Q1's observed behavior" in the abstract — cite the actual experiment.
  - `…::unnumbered_chapter_consumes_no_slot_and_numbers_flat`. RED (ch2 numbered 3/3.1 pre-fix), GREEN after.
- [x] Integration test: a same-chapter `@ref` resolves to a working link with the correct number.
  - `…::same_chapter_ref_resolves_to_working_link`. GREEN after the fix; note the "not unresolved" assertion is scoped to the same-chapter link itself (`class="quarto-xref"` exactly) because the minibook's ch2 deliberately also holds the cross-chapter ref whose unresolved marker the next test pins.
- [x] Integration test: a cross-chapter `@ref` renders a visible, non-crashing "unresolved" indicator (not a broken link, not a panic) — this is the explicit contract P4 ships with; P5 replaces it.
  - `…::cross_chapter_ref_renders_visible_unresolved_indicator`. Passed before and after (documenting): `?fig-one?` text + `quarto-xref quarto-unresolved-ref` class + dangling `href="#fig-one"`.
- [x] Integration test: sidebar/navbar/cover-image/download-tools generated from P1's config translation render correctly for a real multi-chapter book (verifies P1's sidebar reuse *and* P1's download-tools/cover-image config plumbing under real chapter content, not just structurally) — this test now has something real to verify on the P1 side; it did not before this plan's config-level features were scoped in.
  - `…::sidebar_renders_decorated_chapter_links_for_multichapter_book` (verifying — P1's sidebar + P4's seeds already landed; it pins the composed behavior). Asserts: sidebar + book title render; per-chapter links; the current page's link `active`; Q1's `numberChapterHtmlNav` decoration (`chapter-number`/`chapter-title` spans, `1` numeric / `A` letter) sourced from front-matter titles; appendix under the Appendices section. **Scope notes:** navbar — books are sidebar-style in Q1 too, nothing to assert; download-tools — `download_tools()` entries are **inert config** until a sidebar-tools renderer exists (bd-fod3, recorded on the function itself), so no HTML surface exists to assert; cover-image — asserted by this phase's own cover-image item below, not here.
- [x] Integration test: `book.scss` rules appear in the rendered page's compiled CSS.
  - `…::book_scss_rules_appear_in_compiled_theme_css` — RED (no rule in `_book/site_libs/quarto/quarto-theme-*.css`), GREEN after the implementation item below. Asserts ch1 links a `quarto-theme-` stylesheet and that the compiled CSS carries `.sidebar-item .chapter-number` with a resolved `color:` declaration. Paired stage-level unit tests in `compile_theme_css.rs`: `book_project_compiles_book_scss_rules_into_theme_css` (RED then GREEN, same item) and `non_book_project_does_not_gain_book_scss_rules` (passed before the change — documenting the gate).
- [x] Integration test: the cover image renders inside the Preface chapter's first `<section>` (not before it), achieved via AST insertion — confirm no DOM-level fixup was needed.
  - `…::cover_image_renders_inside_preface_first_section` — RED (`the cover image must render on the index page`), GREEN after the implementation item below. Fixture: real 1×1 PNG + `book.cover-image: cover.png` + `book.cover-image-alt` in `_quarto.yml`. Asserts the `<img>` tag (src, `quarto-cover-image`/`nolightbox` classes, `title="Minibook"`), placement after the first section's `<h1>` with the section closing after it, and the file copied into `_book/` by the ordinary resource collector (the Image is in the AST, so no special-cased copy). Pinned observed spelling: `data-fig-alt="Book cover"` — q2's HTML writer data-prefixes custom attributes (writer-level policy, applies to every custom attribute; deviation from Q1's literal `fig-alt`), recorded on the implementation item.
- [x] Unit test: chapter title decoration — a numbered chapter's rendered page title/H1 shows the chapter number prepended; an appendix chapter shows "Appendix A — Title" formatting; an `.unnumbered`/`.unlisted` chapter shows plain, undecorated title (port of `withChapterMetadata`/`formatChapterTitle`) — added by a review pass; this Decision/Implementation item previously had no test of its own.
  - **The port itself already landed** in the seed/injection items above (q2 re-expresses Q1's title-metadata decoration as the header-number injection: the seed offsets the section counter, `inject_header_number` renders the shapes), so these completed as **documenting/pinning tests — passed immediately, recorded as such.** Coverage per case: numbered → new `…crossref_render::seeded_numbered_chapter_h1_decorates_with_chapter_number` (seed 2 → span "2" + title, explicitly no appendix prefix; the pre-existing unseeded `header_section_number_injected_when_stashed` covers plain `number-sections`); appendix → pre-existing `appendix_level1_header_gets_appendix_title_shape` (exact `Appendix <span>A</span> — Title` inline sequence); `.unnumbered` → pre-existing `no_injection_without_number_sections` (render level) + `number_attr_skips_unnumbered_and_non_html` / `unnumbered_header_skips_numbering_but_is_recorded` (index level) + the integration `unnumbered_chapter_consumes_no_slot_and_numbers_flat`. **`.unlisted` — deliberate deviation from this item's wording, Q1-faithful:** new `…::unlisted_class_does_not_suppress_decoration` pins that `.unlisted` does **not** suppress body decoration, because Q1's `isListedChapter` gates only the toc default and `chapterInfoForInput` never consults it — listing is P1's sidebar translation's concern, decoration is the seed's. Gates: included in the next per-crate run.
- [x] Documenting test, matching P2's equivalent: a chapter's front matter sets `subtitle`/`date`/`abstract`; the rendered multi-file HTML page shows none of it. Pins the current (`bd-ock6cyiy`-tracked) silent-drop behavior for multi-file mode too — added by a review pass; P2 already had this test, P4 didn't, though the gap applies equally to both render modes.
  - **Premise corrected empirically — the item as written was wrong about the current behavior.** The drop-test FAILED: multi-file HTML renders each chapter with its own front matter, and the HTML title-block partial (`template.rs`) renders subtitle, date (normalized by `DateNormalizeTransform`), and abstract whenever present. This **matches Q1** — chapter front-matter fields flow into `format.metadata` and render in the chapter page's title block (only `description` is suppressed via `kHideDescription`). P2's drop is a *single-file merge* artifact — non-first chapters' meta cannot survive the merge — not a render-mode rule. Recorded as `…::chapter_front_matter_subtitle_date_abstract_render_on_chapter_pages` (documenting; pins the real Q1-faithful behavior, so a future bd-ock6cyiy decision starts from the truth: multi-file mode already shows these fields; single-file mode does not). No implementation change made or needed.

### Implementation
- [x] Confirm P0's `ChapterSeed`/`RenderContext.chapter_seed`/`with_chapter_seed` mechanism has landed (this phase depends on it, per the epic's phase table — P0 owns building it, this phase only consumes it).
  - Confirmed: `ChapterSeed` + `RenderContext.chapter_seed` at `render.rs:450/:470`, builder `with_chapter_seed` at :575; `CrossrefIndex` seeding at `crossref_index.rs:86-94` (plus `in_appendix` at :135). Empirically re-confirmed by the new tests' H1 halves passing before any P4 change.
- [x] **Figure/table/equation "N.M" chapter-scoped number composition — resolved by a follow-up research round, concrete fix, not an open decision.** `crossref_index.rs` needs no changes — `index_custom_target` already seeds `order.section` for figures/tables/theorems/equations identically to headers. Fix `crossref_render.rs`'s four display functions (`render_float_ref_target`, `render_theorem`, `render_equation`, `render_resolved_ref`) to read `ctx.chapter_seed.chapter_number` directly (not `order.section`) rather than displaying bare `order.order`. Extract one shared helper, `format_crossref_number(order: u32, chapter_seed: Option<&ChapterSeed>) -> String` (`None` → today's exact `"{order}"`; `Some(n, is_appendix: false)` → `"{n}.{order}"`; `Some(n, is_appendix: true)` → `"{letter(n)}.{order}"`), called from all four sites — `prefix_caption`'s `number: Option<u32>` parameter becomes `Option<String>`. Share the letter-vs-numeral conversion with P0's `@sec-` presentation code rather than duplicating it. **Checked by an eighteenth-pass review for the same backwards-ownership shape as the ninth-pass `ChapterSeed` bug — not present here.** P0 runs before P4 and independently commits (its own checklist) to building this letter-vs-numeral branch inline, for its own "Appendix A" `@sec-` test, with no dependency on P4. This item is P4 *extracting* that already-landed inline logic into the shared `format_crossref_number` helper — an ordinary forward refactor of code that exists by the time P4 runs, not P0 assuming P4 plumbing that doesn't exist yet.
  - Landed: `format_crossref_number` in `crossref_render.rs`; `FloatState.appendix: bool` replaced by the full `chapter_seed: Option<ChapterSeed>` (single source of truth — `inject_header_number` now derives the appendix flag from it). The four sites read it: float captions via `prefix_caption(.., Option<String>)`, theorem labels via `theorem_label_inlines(.., Option<String>)`, equations via the `quarto-eq-number` attr, non-`sec` resolved-ref link text. The letter conversion is P0's `format_chapter_index`, now `pub(crate)` and re-exported from `crate::crossref`. Non-book renders are byte-identical (`None` → the exact old `"{order}"`). Gates green: clippy clean; `cargo nextest run -p quarto-core` 5025 passed / 31 skipped (+2 = the two new tests).
- [x] New `chapter_seed: Option<ChapterSeed>` field on `RenderToFileOptions`, applied in `render_document_to_file` right after `RenderContext::new(...)`.
- [x] New `chapter_seeds: HashMap<PathBuf, ChapterSeed>` field on `RenderToFileRenderer` (default empty) plus a `with_chapter_seeds(...)` builder — **not** a new parameter on the `Pass2Renderer` trait's `render_batch` method (revised in the tenth pass; see the Decision above — this keeps `RenderToHtmlRenderer`/`RenderToPreviewAstRenderer` untouched). `RenderToFileRenderer::render`/`render_batch`'s own bodies look up the map per document and, only when a seed exists for that path, pass a cloned-and-overridden `RenderToFileOptions` into `render_document_to_file` instead of the shared `self.options` reference.
  - **Implemented with one deviation from the letter of this item:** the lookup lives in the `render_batch_serial`/`render_batch_parallel` *free functions* and single-doc `render()` (the plan's own implementability wrinkle — those own the per-doc loop; `render`/`render_batch` never see individual documents). The `with_chapter_seeds` construction-time builder was added and then removed: seeds don't exist until after `pre_render` builds the render-item list, so a builder-only API had no caller. Instead the defaulted trait method `Pass2Renderer::install_book_chapter_seeds(seeds)` (no-op base; only `RenderToFileRenderer` overrides) installs the map post-`pre_render` — `RenderToHtmlRenderer`/`RenderToPreviewAstRenderer` still need zero changes.
- [x] Chapter-number/appendix-flag computation in `pre_render`, building the `HashMap<PathBuf, ChapterSeed>` from P1's `BookRenderItem` list, passed into a `RenderToFileRenderer::with_chapter_seeds(...)` call by `run_with_book_support()`'s multi-file branch.
  - `chapter_seed_map(project_dir, items)` in `project/book/render_item.rs`: one seed per file-bearing numbered item, keyed by canonicalized `project_dir.join(file)` (must match `DocumentInfo.input`, which discovery canonicalizes — the macOS `/tmp`→`/private/tmp` trap bit the unit test first). Uniform rule: `number: Some(n)` → seed; `kind` only decides `is_appendix` (matches P1's `chapter_label_prefix`, no Index special case). Unit test `seed_map_covers_numbered_files_and_skips_unnumbered`. Install site: `run_inner` after `pre_render`, gated `if let Some(items) = &self.project.book_render_items` — only books populate the list and only book HTML reaches `run_inner` (Typst/EPUB divert to the merge), so one `if let` serves both gates.
- [x] `CrossrefIndex` seeding call site, using P0's seed capability, reading the newly-seeded `RenderContext` field.
  - P0's `CrossrefIndexTransform` seeding (`crossref_index.rs:86-94`) already reads `ctx.chapter_seed` — no new call site needed; the whole task was getting a real value into that field (the two items above plus the bridge item below).
- [x] **Discovered and fixed: the seed was silently dropped between `render_document_to_file` and the transforms — the ctx bridge chain.** RED integration tests + e2e probe proved the seed reached `render_document_to_file` (instrumented) yet output stayed flat, while unit tests calling transforms directly passed — the exact tests-vs-real-path divergence class this repo has twice documented. Root cause: `chapter_seed` existed on `RenderContext` only. The pipeline copies *selected* fields `RenderContext`→`StageContext` (`stage_context_from_render_context`, `pipeline.rs`), and `AstTransformsStage` then builds a *fresh inner* `RenderContext` bridging its own selected field list (`ast_transforms.rs:219+`). A field not bridged **twice** is invisible to every transform in the real pipeline. Fix (all one-way — read-only input, never mutated, nothing restores it): `StageContext.chapter_seed: Option<ChapterSeed>` field + constructor init; `stage_ctx.chapter_seed = ctx.chapter_seed;` in `stage_context_from_render_context`; `render_ctx.chapter_seed = ctx.chapter_seed;` in `AstTransformsStage::run`.
- [x] Regression test: a non-book render (empty/absent `chapter_seeds`) is byte-identical to before this change, and confirm `RenderToHtmlRenderer`/`RenderToPreviewAstRenderer` needed no changes at all (the trait signature they implement didn't move).
  - `…::non_book_render_numbers_stay_flat` (documenting/pinning — passed before and after the seed work): a `project: type: default` project renders flat `Figure 1` caption + ref, no `header-section-number`/`data-number` anywhere, through the same `run_with_book_support` entry the CLI uses. Transform-level `None`-seed → old bytes was already pinned by the `format_crossref_number` unit tests (item above). Trait: `git diff` shows only the defaulted `install_book_chapter_seeds` (no-op base + `RenderToFileRenderer` override); the two other implementors inherit the default — zero changes to them. Also: the three-chapter test now additionally pins `count_html_outputs == 4` (one file per chapter, the multi-file-ness itself).
- [x] Title decoration port (`withChapterMetadata`/`formatChapterTitle` equivalent) — leave the insertion point extensible for `bd-ock6cyiy` (per-chapter subtitle/author-affiliations/date/abstract, tracked separately), per the Decision above: one block in what could later be a short, ordered sequence, not a single hardcoded block.
  - **Landed as the seed + injection pair recorded on the earlier items** (this item's checklist predates that design settle): the chapter seed offsets `crossref_index`'s section counter, and `inject_header_number` (`crossref_render.rs`) renders Q1's `formatChapterTitle` shapes — number span (`header-section-number`, Q1's `sections.lua` class), the level-1 appendix `Appendix <span>A</span> — Title` arm (with `appendix-title`/`appendix-delim` language terms), and plain content when no `number` kv was stashed (the `.unnumbered` case). Extensibility note for `bd-ock6cyiy`: the decoration inserts at the head of the header's content inline list; per-chapter subtitle/date/abstract would extend the *template's title block* (which already renders those fields from meta — see the documenting-test item above), a separate seam, not a restructuring of this one. The three-case unit coverage lives in the title-decoration unit-test item above.
- [x] `book.scss` bundle inclusion, port of `bookScssBundle`.
  - **Implemented with two deliberate deviations from Q1's shape**, both recorded here: (1) Q1 emits `quarto-book.css` as its own file; q2 compiles the rule **into the fingerprinted theme CSS** (`CompileThemeCssStage`) — the observable contract (rule present in the page's compiled CSS with `$body-color` resolved) is identical and no separate-artifact/link-order plumbing is needed; the content-derived fingerprint keeps book and non-book artifacts distinct automatically. (2) Q1 gates on `formatHasBootstrap(format)`; q2's equivalent gate is two-fold — the stage's revealjs branch returns before the layer is ever attached, and a `theme: none` variant returns `DEFAULT_CSS` before compiling it (no Bootstrap → no `$body-color` to resolve, so the edge is structurally excluded rather than gated). Mechanics: the one-rule Q1 `resources/projects/book/book.scss` is embedded as `BOOK_SCSS_RULES` in `compile_theme_css.rs` (repo external-sources policy: copy, never reference); `run()` appends it to `doc_vars.rules` when `ctx.project.project_kind() == ProjectKind::Book` — as the last user layer, its rules section compiles after all Bootstrap defaults, so `$body-color` resolves. **Cache safety:** `cache_key` now hashes `doc_vars.rules` when non-empty (`doc_rules:` segment, book-only) — without it a cached non-book compile of the same themes/doc-vars would serve a book page without the rule and vice versa; hashing only when non-empty keeps every existing rules-free cache key (and its entries) valid, so no mass invalidation. Gates green: clippy clean; `cargo nextest run -p quarto-core` 5035 passed / 31 skipped (phase total now +12 vs the 96c5d90d3 baseline: 7 integration + 2 crossref unit + 1 render_item unit + 2 theme unit; the interim per-crate figures recorded on earlier items were mid-arc snapshots at smaller test-set states — the final census is this one).
- [x] Cover-image AST insertion (not DOM postprocessing) for the index/Preface chapter.
  - `BookCoverImageTransform` (`transforms/book_cover_image.rs`, Normalization phase) pushed immediately before `TitleBlockTransform` in the non-revealjs arm, so `SectionizeTransform` wraps the inserted `Paragraph[Image]` inside the first section right after its header — Q1's post-`bookHtmlPostprocessor` shape with **no DOM fixup ported** (the DOM postprocessor half is deliberately not ported, per the Decision above). Gates: book project + document `file_name()` starts with `index.` (Q1's `isBookIndexPage`). Sources in Q1's precedence: doc meta `cover-image` (declared-file-relative base), then `book.cover-image` from project config (project-dir base); leading `/` = project root per the path-resolution contract; the absolute source is re-expressed document-relative (or `/`-root-relative) as the Image target, and the ordinary `ResourceCollectorTransform` (Finalization, later) copies the file — no custom copy code. Empty alt text keeps it a plain `<p><img>` (no implicit figure); `cover-image-alt` becomes a `fig-alt` attribute. **Two recorded findings/deviations:** (1) q2's HTML writer data-prefixes custom attributes, so `fig-alt` ships as `data-fig-alt` in HTML (writer-level policy, all custom attributes — Q1 emits the bare, technically-invalid attribute); (2) Q1 sources the img `title` via `withBookTitleMetadata`; q2's multi-file path does not merge the book title into per-page meta by transform time (verified: `ast.meta.get("title")` is `None` on the index page), so the transform reads the page's own title first, falling back to `book.title` from project config, and omits the attribute entirely when neither exists (Q1 emits an empty one). **Registration side-effects pinned by repo invariant tests, all updated:** `BUCKETS` B2 entry, `PANDOC_TRANSFORM_EXCLUDED` entry (the Pandoc cut must not bake HTML presentation), and the two exact-name-list pin tests (`t1_4_html_render_profile…`, `t2_5_html_preview_profile…` — the preview pipeline shares the arm and picks the transform up too, which is correct for P8's book previews). Gates green: clippy clean; `cargo nextest run -p quarto-core` 5036 passed / 31 skipped (+1 = the cover test; phase total now +13 vs the 96c5d90d3 baseline).
- [x] `run_with_book_support()`'s multi-file-HTML branch.
  - **Already landed as a side effect of the install site, recorded for completeness.** The item predates the design settle: the multi-file HTML branch exists via `run_inner`'s chapter-seed install (gated on `book_render_items`, which only book HTML populates — see the seed item above) and the ordinary Pass-2 per-file render; there is no separate "branch" to write. Every integration test in this file exercises it end-to-end.
- [x] `cargo clippy -p quarto-core --all-targets -- -D warnings`; `cargo nextest run -p quarto-core`; phase-boundary `cargo nextest run --workspace` with delta reported.
  - All three green. Per-crate: clippy 0 errors; `cargo nextest run -p quarto-core` **5039 passed / 31 skipped**. Phase-boundary workspace: `cargo nextest run --workspace` **14892 passed / 200 skipped** vs the live baseline **14876 passed / 200 skipped @ 96c5d90d3** → **+16 passed, 0 delta skipped**, accounted for exactly by the quarto-core census: 9 integration (`book_multifile_html.rs`: 7 original + cover-image + chapter-front-matter documenting), 2 crossref P4 unit (`format_crossref_number`), 1 render_item unit, 2 `compile_theme_css` unit, 2 title-decoration unit. No other crate's count moved.
  - **End-to-end (real CLI, output inspected):** `cargo run --bin q2 -- render .tmp-p4-cover --to html` on a 2-chapter book with `cover-image: cover.png` + `cover-image-alt: "Book cover"` (project config) rendered 2/2 files. `.tmp-p4-cover/_book/index.html` contains `<img src="cover.png" alt="" class="quarto-cover-image nolightbox img-fluid" data-fig-alt="Book cover" title="P4 Probe Book" />` placed **inside the first `<section>` immediately after its `<h1>Welcome</h1>`** (verified by index arithmetic around the img), and `_book/cover.png` exists (67 bytes — the ResourceCollector copy). Consistent with the integration test's assertions.

## Appendix: the experiments that ground this phase's numbering contract

Built a minimal 2-chapter + 1-appendix HTML book (`scratchpad/minibook`) and rendered it with the real `quarto` binary. Observed, directly:

- Chapter 1's own subsection: `header-section-number="1.1"`. Chapter 2's figure: `Figure 2.1`. Appendix figure: `Figure A.1` — appendix letters are a separate sequence from the numeric chapters (not continuing as "4").
- A cross-reference from the appendix back to chapter 1/2 figures showed their **original** numbers (`1.1`, `2.1`) with correctly fixed-up cross-file `href`s (`chapter1.html#fig-a`) — this is the *reading* side P5 must reproduce (P4 itself doesn't attempt cross-file resolution).
- `@sec-ch1` (an id on chapter 1's own H1) rendered as plain "Chapter 1" text, not "Section 1" — confirms the "Chapter N" presentation convention P0 needs to support.
- Title/nav decoration (`<span class="chapter-number">A</span> <span class="chapter-title">Appendix Stuff</span>`, "Appendix A — Appendix Stuff") matched `numberChapterHtmlNav`/`formatChapterTitle` exactly.

**Second experiment, added after a review pass questioned whether the unnumbered-chapter claim was actually observed**: inserted an `.unnumbered` chapter (`chapter1b.qmd`, with its own figure) between chapter 1 and chapter 2 and re-rendered. Observed:

- The sidebar/pagination still labels the surrounding chapters "1" and "2" — the interlude is invisible to the chapter sequence.
- Chapter 2's figure is still `Figure 2.1`, not `Figure 3.1` — confirms no slot was consumed.
- The interlude's own figure rendered as plain `Figure 1` — flat, unscoped, not chapter-relative in any form. This is a real, specific finding (not derivable from the first experiment, which only tested the Preface/index page — a different `BookRenderItem` kind that's excluded from chapter numbering for an unrelated reason) and is now what this phase's test actually asserts, rather than a generalization from a different case.
