# Plan: Pass 3 — project-wide crossref registry (book-projects P5)

**Date:** 2026-09-21 (revised after a critical review pass; renumbered from a prior draft's P6 — see the epic doc's revision history)
**Epic:** [`2026-09-21-book-projects-epic.md`](2026-09-21-book-projects-epic.md)
**Design (authoritative):** [`../designs/book-projects-architecture.md`](../designs/book-projects-architecture.md) §8
**Prior art:** `crates/quarto-core/src/crossref/index.rs`'s own module doc (*"per-file indices can be persisted... and merged into a project-wide index... see Phase 4 of the design plan"*) and `claude-notes/plans/2026-04-15-crossref-design.md`'s "Phase 4 — Multi-file foundations" section, whose own O8 explicitly deferred appendix numbering "to the book-projects session" — this epic.
**Q1 reference (for the problem shape, not the mechanism — Q1's DOM-postprocessing approach is explicitly not being ported):** `book-crossrefs.ts`'s `bookCrossrefsPostRender`.

## Overview

This is the epic's most novel piece: resolving a cross-chapter `@ref` (one chapter references another's figure/table/theorem) without DOM postprocessing, which this repo's architecture policy prohibits. The replacement is an AST-level transform consulting an in-memory, project-wide registry — the same pattern `crates/quarto-core/src/transforms/link_rewrite.rs` already established for cross-document *link* rewriting in the website epic, extended from explicit hrefs to crossref ids.

## Decisions

- **"Pass 3," not "Pass 2a/2b."** The pause point is a real, fixed, already-existing seam — but a precise one, not the loose "Crossref/Finalization boundary" an earlier draft claimed. Grepping every `TransformPhase::Navigation` implementor (`sidebar_generate/render`, `navbar_generate/render`, `toc_generate/render`, `footer_generate/render`, `page_nav_generate/render`, `breadcrumbs_render`, `listing_generate/render`, and others) confirms all of it is page-local, config- or local-heading-structure-driven, and must run **before** the pause — Pass 2 runs Normalization, Crossref, *and* Navigation per chapter, then pauses immediately before Finalization. Confirmed against the actual precedent this design copies: `link_rewrite.rs` (cross-document link rewriting) has `phase() -> TransformPhase::Finalization`, not Navigation — Finalization, after Navigation, is where cross-document resolution has always belonged in this pipeline. Book rendering pauses there, aggregates, and resumes — it does not re-run engines a second time (that would double the actual expensive part of rendering; ruled out explicitly during design).
- **Mechanism, redesigned by a sixteenth-pass "step back and consider alternatives" conversation: the pause side is `execute_range` as before; the resume side is a second, previously-missing mechanism, not just a mirror-image `execute_range` call.** The pause (`..=Navigation`) reuses P2's `AstTransformsStage::for_range`/`build_html_pipeline_pause_stages` exactly as before — `TransformPipeline::execute_range` itself needs no new pipeline-construction machinery, and there's no new `PipelineData` enum variant, since this pause is between two `AstTransform` phases operating on the same `(Pandoc, RenderContext)` signature throughout.

  **The resume is not "the same call with a different range" — it needs a real stage list of its own, and a real story for how the output reaches disk, neither of which existed in any earlier draft of this plan.** `AstTransformsStage::run()` only ever runs *inside* the larger stage list `build_html_pipeline_stages_with_options` builds; there is no existing way to start a *fresh* stage-list call mid-way through it. The fix: reuse P2's `build_html_pipeline_finishing_stages(lower_bound)`/`run_pipeline_from_ast` — a general mechanism P2 builds first (it needs its own instance, `Crossref..`, for its merge step) — called here with `Finalization..`. This starts directly with a range-bounded `AstTransformsStage`, then the real, unchanged tail (`UserFiltersStage::post → ResourceReportStage → CodeHighlightStage → MathJsStage → RenderHtmlBodyStage → ApplyTemplateStage`), and returns a real `RenderOutput`.

  **Getting that `RenderOutput` onto disk is this phase's own new work, not reused from P2.** P2's targets (Typst, EPUB) write inside their finishing pipeline's own tail stages (`PandocWriteStage`/`TypstCompileStage`) — P2 never needs a separate write step. This phase's target is HTML, where writing is *not* a stage — it's the external `OutputSink` glue in `render_document_to_file` (`render_to_file.rs:453-472`, entirely outside `run_pipeline`). **Split `render_document_to_file` into its two already-implicit halves for real**: a "render" half (unchanged — resolve paths, build context, call the pipeline, return `RenderOutput`) and a new `write_rendered_output` function (build `OutputSink`, drain `ctx.artifacts`/`resource_copies`/`resource_report`, `sink.write`, `sink.flush`) that existing single-shot callers call right after the render half — byte-identical behavior, a refactor not a redesign. This phase's resume step then: reconstructs `RenderContext<'_>` from the held owned struct → calls `run_pipeline_from_ast(ast, ctx, build_html_pipeline_finishing_stages(Finalization..))` → gets a `RenderOutput` → calls the *same* `write_rendered_output`, once per resumed chapter.
- **P5's per-chapter owned struct — held simultaneously, one per chapter, in a book-scoped `Vec`.** Traced end to end against real code (design doc §6): the pause-side entry point takes `ctx: &mut RenderContext<'_>`, so **the caller constructs `RenderContext`, not the entry point** — nothing ever needs to hold a *constructed* `RenderContext<'a>` across the pause. P5's book orchestrator holds one plain owned struct per chapter (`doc_info`, `binaries`, plus copies of `RenderContext`'s owned fields — the **full** audited list, unlike P2's shorter one; see the checklist below and design doc §6 for why P5 needs every field P2 doesn't), built up front and kept alive until each chapter resumes; `RenderContext<'_>` is reconstructed fresh, borrowing from that struct, for the pause-side `execute_range(..=Navigation)` call and again for the resume-side `run_pipeline_from_ast(Finalization..)` call, then discarded. **Also recompute, don't carry, four more per-chapter values at resume time**: `resolver`, `output_path`, `resource_paths`, `project_type` — all cheap, deterministic functions of `input_path`/`format`/`project` in today's `render_document_to_file`, needed by `write_rendered_output` but not worth adding to the carry-list.
- **`run_with_book_support()`'s multi-file-HTML branch is where this lives, not a general orchestrator concept — and not a `ProjectType` trait method at all, resolved by a seventeenth-pass research round (design doc §3).** `ProjectType` gains nothing new; `run_with_book_support()` is a concrete method on `impl<'a> ProjectPipeline<'a, RenderToFileRenderer<'a>>`, branching directly on `ProjectKind::Book`. No `Checkpoint` associated type, no `Pass3Renderer` trait, no `needs_pass_three()` flag anywhere. `DefaultProjectType`/`WebsiteProjectType` are unaffected. If Manuscript ever needs a multi-step render, it gets its own seam in its own shape — not this one, since Manuscript's actual Q1 shape (independent article + notebooks + MECA packaging) doesn't fit this mechanism at all.
- **In-memory now; keep types serializable for later.** The crossref-design plan's own Phase 4 sketch proposed disk persistence (`.quarto/xref/<file-id>.json`). That's the right shape once a freeze-style engine-result cache exists (so re-deriving a chapter's AST for Finalization is a cache hit, not a re-execution) — Q2 has no such cache yet. Define `ChapterCrossrefInventory`/`ProjectCrossrefIndex` as `Serialize`/`Deserialize` (matching `CrossrefIndex` already being so) even though nothing serializes them in this phase, so the upgrade path is a drop-in later, not a redesign — and actually test the round-trip, not just declare the derive.
- **Shape the merge type so it directly serves P8's `StaticProjectAnalyzer` (preview) consumer too — no longer a hypothetical, P8 is now a real phase in this epic.** Same `ProjectCrossrefIndex` type, same `RenderContext` field (see the new Decision below), built from a cheaper pre-engine sweep instead of Pass 2's post-engine one. This phase builds the real producer; P8 only needs to populate the same field from a second, cheaper source and confirm the consuming transform doesn't care which produced it.
- **The registry entry shape**: id → `{owning_chapter_href, resolved_number (formatted string, e.g. "2.3"), caption_inlines, ref_type}`. Built from each chapter's `ChapterCrossrefInventory` (its own `CrossrefIndex.entries`, harvested at the pause point). **Correction, found by an eighteenth-pass review: `resolved_number` is not "already computed by the existing `CrossrefIndexTransform`" — the aggregation step must compute it itself.** `CrossrefIndexTransform` (Crossref phase, before the pause) only populates each entry's *raw* `Order` (the `order.order`/`order.section` counters); the *formatted* string is produced exclusively by design doc §7's `format_crossref_number(order, chapter_seed)` helper, called from `CrossrefRenderTransform`'s display functions — a **Finalization**-phase transform that hasn't run for any chapter yet at this phase's pause point (immediately after Navigation). The aggregation step must call `format_crossref_number` itself, once per harvested entry, using that chapter's raw order data plus its own `ChapterSeed` (both already present in the paused checkpoint) — this is real, previously unstated aggregation work, not "just aggregation of what P4 already produces per chapter."
- **How the transform reaches the registry — a new `RenderContext` field, not a book-only pipeline splice (a real gap found by a review pass: this plan previously stated the transform's phase and position but never said where it's registered).** `CrossChapterCrossrefResolveTransform` is registered as an ordinary member of the shared `build_transform_pipeline` (Finalization phase, before `CrossrefRenderTransform`) — not spliced into some book-only pipeline construction — and no-ops (leaves `CrossrefResolvedRef` nodes exactly as `CrossrefResolveTransform` left them) whenever its `RenderContext` field is absent. That field — **`cross_chapter_crossref_registry: Option<Arc<ProjectCrossrefIndex>>`** — follows the same builder idiom as `chapter_seed`/`project_index` (`with_cross_chapter_crossref_registry(...)`), populated by this phase's resume orchestrator right before each chapter's `execute_range(Finalization..)` call. Because the transform only ever touches nodes already marked unresolved by `CrossrefResolveTransform`, it cannot clobber a chapter's own correct local resolution — resolution precedence (local wins, the registry is fallback-only) is safe by construction, not something the transform needs to check explicitly. Registering it unconditionally in the shared pipeline (rather than book-only) is what makes it a true no-op for every non-book render **and** what lets P8 reuse it verbatim for preview, feeding the same field from a cheaper, pre-engine-only producer instead of this phase's real post-engine aggregation.
- **Deliberately serial in this first cut — stated explicitly here, not only in the epic doc's Known Limitations** (added by a review pass: this fact previously existed nowhere in this plan itself, so an implementer working from this file alone had no signal it was intentional). Both halves of the pause/resume round-trip (render-through-Navigation, resume-from-Finalization) look independently parallelizable per chapter, matching `render_batch_parallel`'s shape — but get the aggregation step correct serially first; parallelize as a deliberate follow-up once proven correct, not in the same change. Contrast P4, which explicitly keeps `Pass2Renderer`'s existing parallel `render_batch*` machinery for its own (non-paused) per-chapter renders — this phase's *aggregation/resume* step is the part that's serial, not chapters' initial Pass 2 render through Navigation.
- **Accepted v1 limitation, resolved with the user (eighteenth pass): the resume/write loop is not atomic across chapters.** Because aggregation builds the whole-book registry from every chapter *before* any chapter resumes, chapter A can be written (embedding a resolved link/number into chapter B) before chapter B's own resume-and-write happens; if B's write then fails, A's already-written file references a chapter that was never produced, until the next full re-render. The overall render still reports failure (the error propagates) — this isn't silent, just undocumented until now. Document as a Known Limitation (epic doc), not a blocker; see design doc §8 for the full analysis and the future-mitigation sketch (buffer all N `RenderOutput`s, defer every write until all N resumes succeed) if this ever becomes a real complaint.
- **Forward-compatibility note for P6**: P6's bibliography merge wires a new `ChapterCitationManifest` into "P5's per-chapter held struct/`Vec`... the same way `ChapterCrossrefInventory` does" (see P6's plan) — build the held-checkpoint type so a field can be added later without restructuring the pause/resume mechanism itself (e.g. don't hard-code it as a fixed-arity tuple `(BookRenderItem, Pandoc, RenderContext, ChapterCrossrefInventory)` that's awkward to widen; a struct with named fields extends more easily). Whichever of P5/P6 lands first should leave this room, not assume the other will restructure it later.
- **One dependency on P4, both now resolved by research but still a real sequencing requirement.** This phase's aggregation step reads whatever chapter-scoped numbers P4 actually produces for figures/tables/equations. P4's `order.section`-vs-`order.order` gap (design doc §7) now has a concrete fix (read `ctx.chapter_seed.chapter_number` directly in `crossref_render.rs`'s display functions) — confirm P4 has actually *landed* that fix before relying on its output here; a resolved design isn't a shipped implementation. (The `render_documents`/`Box<dyn ProjectType>` object-safety dependency this bullet used to name is fully resolved, not just designed — `run_with_book_support()` is a concrete method, so there's no trait signature to wait on; see design doc §3.)

## Checklist

### Tests first
- [x] Unit test: aggregating 3 chapters' `ChapterCrossrefInventory` (each with distinct crossref ids) into a `ProjectCrossrefIndex` produces the expected id → (href, number, caption) map, with no chapter needing to know about any other during its own local pass. (`aggregates_three_chapters_into_a_project_wide_registry`, observed RED against a stub aggregation before implementation.)
- [x] Unit test: `ChapterCrossrefInventory`/`ProjectCrossrefIndex` round-trip through `serde_json` (serialize, deserialize, compare) — proves the "serializable from day one" decision is real, not just a derive that's never been run. (`chapter_and_project_types_round_trip_through_serde` — recorded as a **pinning test**: it passed immediately via the derives; it guards the forward-compatibility contract rather than driving behavior.)
- [x] Unit test: a duplicate id across two chapters produces a diagnostic identifying both locations (mirrors the existing single-document duplicate-id diagnostic's shape and O7's resolution in the crossref design plan), not a silent last-wins overwrite. (`duplicate_id_across_chapters_is_diagnosed_and_first_wins`, observed RED against the stub; new code **`Q-15-2`** — catalog entry, `docs/errors/crossref/Q-15-2.qmd`, and sidebar entry added in the same change; keep-first policy mirrors `Q-15-1`.)
- [x] Integration test: chapter 1 references `@fig-x` defined in chapter 3 — after the full render, chapter 1's HTML contains the correct number and a working relative link into chapter 3's actual output file. Verified by reading the rendered HTML file from disk, per this repo's end-to-end verification rule — not by inspecting the AST alone. (`cross_chapter_forward_reference_resolves_with_number_and_link` in `book_multifile_html.rs` — ch1 forward-references `@fig-two` **and** appendix `@fig-app`, asserting `href="ch2.html#fig-two"` + `>Figure\u{a0}2.1</a>` and `href="app-a.html#fig-app"` + `>Figure\u{a0}A.1</a>`. **Observed RED** against P4 behavior: ch1 rendered the `?id?` placeholder with a dangling `href="#fig-two"`.)
- [x] Integration test: an id that exists in *no* chapter still produces the P4-established "unresolved" placeholder (not a crash, not a silently-wrong number) — confirms P5 is additive over P4's contract, not a replacement of its error handling. (`unknown_ref_still_shows_unresolved_placeholder` — written and **passing immediately**: it pins the P4 contract that must *survive* P5; it stays in the suite to fail if the registry transform ever touches an id it doesn't know.)
- [x] Integration test: the pause/resume round-trip is verified to *not* re-run engine execution — instrument a test fixture with a counter-incrementing code cell and assert it executes exactly once per chapter across the full P4→P5 flow. (`engine_executes_exactly_once_per_chapter_across_pause_resume` — implemented with P2's `FixtureEngine` rather than a real interpreter: a `{fixture-a}` cell in each of ch1/ch2, the engine's shared `received_inputs` handle counts every `execute` call across the whole book render, asserted `== 2`; a second execution pass would read 4. Also asserts each chapter's HTML carries the spliced result. **Passing immediately** — P4 executes once today; the test's job is to go RED if the resume leg ever re-runs engines.)
- [x] Integration test: sidebar/navbar/TOC output (Navigation phase, run before the pause) is unaffected by anything the aggregation step or the resumed Finalization phase does — confirms the phase split didn't leak Navigation-phase work across the boundary. (`navigation_phase_output_survives_cross_chapter_resolution` — sidebar existence + decorated chapter-number labels + per-page active state asserted on two chapters. **Passing immediately** (guard): a finishing-stage range accidentally starting at Navigation would regenerate or drop this markup.)
- [x] Regression test: a non-book (or single-file-merge) render never populates `cross_chapter_crossref_registry`, and `CrossChapterCrossrefResolveTransform` is a true no-op in that case — output byte-identical to today's pre-P5 behavior. This is the guard that makes registering the transform in the *shared* `build_transform_pipeline` (rather than a book-only pipeline) safe. (`non_book_render_is_byte_identical_without_the_registry` — pins the complete xref markup on a default-project page: the full `<a href="#fig-solo" class="quarto-xref">Figure\u{a0}1</a>` anchor, the caption, and no `quarto-unresolved-ref`. **Passing immediately** (guard); the transform's own no-op-when-unregistered unit test lands with the transform below.)
- [x] Unit test extending P2's owned-struct/reconstruction round-trip test to P5's harder case: build 3 chapters' owned structs simultaneously (not one at a time, per P2's drop-immediately pattern), call the pause-side entry point on each with `..=Navigation`, then resume each independently via `run_pipeline_from_ast(Finalization..)` — assert each chapter's own owned fields (**the full, audited list — unlike P2's, which needs only a short subset, this phase genuinely needs all of it**: `artifacts`, `diagnostics`, `crossref_index`, `ref_type_registry`, `project_index`, `resource_resolver`, `options`, `includes`, `pipeline_profile`, `observer`, `user_grammar_provider`, `resource_report`, `resource_copies`, `code_block_decorations`, `attribution_provider`, `attribution_data`) come back correctly attributed to *that* chapter, not cross-contaminated with another chapter's held state. This is the regression guard specific to P5's "N structs alive at once" requirement, which P2's own version of this test (a single struct, used and dropped) can't exercise. `attribution_data`/`code_block_decorations`/`crossref_index` are the sharpest cases here specifically: all three are read only in the *resumed* Finalization phase (or, for `crossref_index`, by this phase's own aggregation step before the resume), so a bug that drops them during the pause wouldn't surface until later, not immediately. (Landed 2026-09-25. The struct is **`BookChapterPauseState`** (`pipeline.rs`, next to `ChapterPauseState`): the 16 audited fields + P2's output-only `execution_skipped` + `format_options`/`document_profile` (both proven by the item-45 bridge round-trip to survive a resume only if the caller re-seeds them), with `new`/`build_context`/`extract_from` mirroring P2's API. Deliberately **not** serde-derivable (`observer`/`user_grammar_provider` are trait objects) — the serializable-from-day-one decision applies to the inventory types, not live render state. Named-field shape is the P6 forward-compat contract (`ChapterCitationManifest` becomes one more field). Not carried, on purpose: the orchestrator-controlled inputs set fresh per reconstructed context — `chapter_seed`, `defer_citeproc`, `execution_policy`, `engine_registry_override`, `cross_chapter_crossref_registry` — plus the borrowed project/document/format/binaries. Test: `book_chapter_pause_state_holds_three_chapters_without_cross_contamination`, **observed RED** against a deliberately P2-short build/extract (the naive pattern extension the guard exists to catch) at "ch1: attribution_data must round-trip as the chapter's own sidecar"; green once the full list is carried. Test-shape note: a first draft seeded `format_options.html.attribution_viewer_enabled` as its distinct marker and caught real pipeline behavior, not a bridge bug — `AttributionRenderTransform` legitimately rewrites that field during the resume (from meta). Restructured: ch3 runs attribution-off (sidecar `None`, transform early-returns) with a seeded `viewer_enabled = false` nothing touches; ch1–2 assert the transform's lookup writes land *on* the round-tripped struct.)
- [x] **Resolved before implementation, per the user's explicit ask**: confirm `AttributionGenerateStage` (populates `RenderContext.attribution_data`, if a provider is installed) genuinely cannot double-fire across this phase's two calls. It's a plain `PipelineStage`, not a `TransformPipeline`/`AstTransform` member (`pipeline.rs:6344`'s `t6_3_attribution_generate_is_stage_only_not_a_transform_pipeline_member` pins this structurally; verified 2026-09-25) — it runs once, in the initial pre-pause stage-list call, entirely outside what `execute_range` iterates over and entirely outside the resume-side finishing pipeline (which starts fresh at `Finalization..`, never re-running it). No new mechanism needed; the only real risk this surfaced was the `attribution_data` carry-list gap (fixed in the item-45 bridge work), not a control-flow one. See design doc §8 for the full trace.
- [x] Integration test: `run_pipeline_from_ast(Finalization..)`'s own `RenderContext↔StageContext` bridge round-trips this phase's full field list correctly, both in (at the resume call) and out (before `write_rendered_output` reads `ctx.artifacts`/`resource_copies`/`resource_report`) — this is the concrete fix for the bridge-completeness gap an implementability review found (`run_pipeline`'s existing bridge only carries ~8 of 16 fields, because no existing caller ever needed more); confirm it's actually complete for *this* phase's needs, not just declared so. (Two tests in `pipeline.rs`, both **observed RED** first. `finishing_pipeline_bridge_round_trips_seeded_fields`: pauses a code-block-filename doc at `..=Navigation`, seeds `includes`/`format_options`/`document_profile` on the held context, resumes via `run_pipeline_from_ast(Finalization..)`; asserts the probe header-include reaches the HTML (`includes`), the paused code block still wraps in `code-with-filename` (`code_block_decorations` survives pause→generate-side harvest), and the seeded options/profile survive the round trip. `finishing_pipeline_bridge_carries_attribution_data_both_ways`: same pause/resume with `attribution_data = Some(...)`, asserts the resumed `AttributionRenderTransform` sees the bridged sidecar and bakes the writer-side lookup. **Bridge fixes, all driven by these REDs:** a new `StageContext.code_block_decorations` field (`stage/context.rs`); outer in-bridge `stage_context_from_render_context` gains `includes` (take), `code_block_decorations` (take), `attribution_data` (clone), `format_options` (take), `document_profile` (take); outer restore `restore_render_context` gains the three take-backs (attribution/profile/`format_options` were already carried out — the gap was seeding them *in*); inner `AstTransformsStage` bridge gains the `code_block_decorations` in/out legs and the `format_options` in-move. The test caught one real additional bug: the inner out-leg's existing unconditional `ctx.format_options = render_ctx.format_options` **clobbered the seeded value with the inner context's default**, because the inner in-bridge never transferred `format_options` in — fixed by the in-move (mirrors `code_block_decorations`), which also makes `AttributionRenderTransform`'s writes land on the same struct the stage context carries out rather than replacing pre-seeded values. `document_profile` is outer-bridge-only (transforms never see it), which the round-trip pins. Stale "no StageContext bridge needed" comment in `code_block_generate.rs` corrected to note the P5 pause/resume split. Both tests green after the fixes.)
- [x] Integration test: `write_rendered_output`, called from this phase's resume step, produces a real HTML file at each chapter's own (recomputed, not carried) `output_path`, with artifacts/resource copies/resource report correctly drained — and confirm the *existing* single-shot `render_document_to_file` callers are byte-identical after being split into "render" + "write" halves (regression guard on the refactor itself). (`resume_finalize_writes_real_html_and_drains_sidebands` in `render_to_file.rs`'s test module — runs the orchestrator's exact per-chapter sequence against a real temp dir: pause at `..=Navigation` via `render_qmd_to_ast_partial`, hold via `BookChapterPauseState::extract_from`, recompute the output trio with `determine_output_paths` + build the `ResourceResolverContext::website` resolver, resume via `run_pipeline_from_ast(Finalization..)`, then `finalize_rendered_output(is_native = true)`. Asserts real chapter HTML at the recomputed `output_path` including the resumed decoration wrapper, the page-scoped artifact flushed at its `resolver.on_disk_path_for` route, the resource-copy intent flushed at its dest, and the resource report drained into the result. **Passing immediately — a guard, not a behavior driver**: the machinery (P2's extracted finalize) already existed; the test's job is to prove the write path end-to-end *before* the orchestrator relies on it. The byte-identical half needs no new test: the split landed in P2 as a pure Extract Method and every render since — the whole P2–P4 suite plus e2e — exercised the split path.)

### Depends on
- [x] P2's `AstTransformsStage::for_range`, `build_html_pipeline_pause_stages`, and `build_html_pipeline_finishing_stages`/`run_pipeline_from_ast` have landed — this phase reuses all of them rather than building parallel versions. (Verified 2026-09-25: `ast_transforms.rs:109`, `pipeline.rs:634`, `pipeline.rs:644`, `pipeline.rs:930`.)
- [x] P4's `order.section`-vs-`order.order` figure/table numbering fix (design doc §7, resolved: `crossref_render.rs`'s display functions read `ctx.chapter_seed.chapter_number` directly) is actually implemented, not just designed — confirm before relying on P4's per-chapter numbers in this phase's aggregation. (`format_crossref_number` at `crossref_render.rs:400` composes from `chapter_seed`; pinned by P4's `chapter_seed_offsets_section_numbering`.)
- [x] P1's `run_with_book_support()` (design doc §3, resolved) exists on `ProjectPipeline<'a, RenderToFileRenderer<'a>>` — this phase's multi-file-HTML branch lives inside it. (`orchestrator.rs:1013`.)

### Implementation
- [x] `ChapterCrossrefInventory`/`ProjectCrossrefIndex` types (serializable from day one, per the decisions above). (`crates/quarto-core/src/crossref/project_index.rs`; `aggregate_chapter_inventories` computes `resolved_number` itself — floats/equations via `format_crossref_number` with the owning chapter's seed, `sec` via `format_section_number` on the seed-offset section path — matching the eighteenth-pass correction. Registry entries also carry raw `order` + `in_appendix` beyond the plan's four named fields, so the resumed display path can patch them onto resolved nodes for the `sec` kind swap. One semantics note: `format_section_number` renders the seed-offset top component **only when `max_heading == 1`** — book chapters always run there (crossref.chapters forces it), which the `sec_entries_format_full_section_paths_in_book_chapters` test pins. `format_crossref_number` was widened to `pub(crate)` (and `transforms::crossref_render` to `pub(crate) mod`) so the aggregation can call the one canonical composer.)
- [x] Split `render_document_to_file` into a "render" half (unchanged) and a new `write_rendered_output` function (the existing `OutputSink`/artifact-drain/`sink.write`/`sink.flush` tail, `render_to_file.rs:453-472`) — existing callers call both back-to-back. **This phase builds the split; P2 doesn't need it** (its targets write inside their own finishing-pipeline tail stages). **Pre-satisfied by P2**: the extraction already landed as `finalize_rendered_output` (`render_to_file.rs:463`, `pub(crate)`), a pure Extract Method whose only single-shot caller (`render_document_to_file`, `render_to_file.rs:430`) calls render-then-finalize back-to-back exactly as this item describes. The plan's `write_rendered_output` name is realized by that function — no duplicate was created and the P2-landed name is kept (it also flushes artifacts and assembles the result, so "finalize" is the more accurate verb).
- [x] `run_with_book_support()`'s multi-file-HTML branch, extended: Pass 2 (P4) now stops after `Navigation` via P2's `AstTransformsStage::for_range`/pause-stage constructor instead of finishing; a new aggregation step builds the registry; a resume step, per chapter, reconstructs `RenderContext`, calls `run_pipeline_from_ast` with `build_html_pipeline_finishing_stages(Finalization..)` to get a `RenderOutput`, recomputes `resolver`/`output_path`/`resource_paths`, then calls `write_rendered_output`. (Landed 2026-09-25. The branch is `run_book_multi_file_html` (`orchestrator.rs`, dispatched from `run_with_book_support`'s `FormatIdentifier::Html` arm; the whole `RenderToFileRenderer` impl — including the book branches — is native-only `cfg(not(wasm32))`, so WASM keeps its existing fall-through), implemented in the new `project/book/multi_file_html.rs` (registered in `book/mod.rs`): `render_book_multi_file_html(project, book_items, format_str, index, runtime, project_artifacts, render_options, format_override, fail_fast) -> (outputs, failures, aggregate_diagnostics)`. Per chapter: pause leg mirrors `render_document_to_file`'s format resolution and output-trio/href computation, renders `render_qmd_to_ast_partial(..=Navigation)` with the seed/project_index/resolver/engine flags/attribution on the context, harvests `state.crossref_index` into a `ChapterCrossrefInventory`, and holds everything in a per-chapter `HeldChapter` (the `BookChapterPauseState` + paused AST + recomputed paths/format). Non-native chapters (e.g. a `pdf`-format doc inside an HTML book) and non-item project files (additions/404 pages) render via plain `render_document_to_file`, exactly as P4's fall-through did. Aggregation: `aggregate_chapter_inventories`. Resume leg: `build_context` → extend pause diagnostics → re-apply seed → `with_cross_chapter_crossref_registry` → `build_html_pipeline_finishing_stages(Finalization..)` → `run_pipeline_from_ast` → `finalize_rendered_output(is_native = true)`; `execution_skipped` ORs the held state with the resumed context's; `fail_fast` breaks both loops. Orchestrator side: pass 1 → fail-fast early return (with drained diagnostics) → `pre_render` → the loop (threading `self.renderer.format_override()` and `self.renderer.options`) → `post_render` gated on `is_html_based()` with a built project resolver (book pages get sitemap/robots/redirects/feeds like `run_inner`'s HTML path) → full resource/manifest/`post_resources` tail → drain → `stopped_early = fail_fast && !pass2_failures.is_empty()`. **TDD**: both integration drivers flipped first and observed RED together (2 run / 0 passed, `book_multifile_html.rs:741`/`:604` as the file stood at the flip): `cross_chapter_forward_reference_resolves_with_number_and_link` (RED recorded at item 38) and P4's `cross_chapter_ref_renders_visible_unresolved_indicator`, flipped to P5 expectations (`>Figure\u{a0}1.1</a>`, `href="ch1.html#fig-one"`, no `quarto-unresolved-ref`). **Bridge-gap discovery**: the flip's first GREEN attempt failed the item-43 round-trip guard at the attribution sidecar — the resume's `RenderContext↔StageContext` bridge was missing the in-legs for fields only the resumed Finalization reads (`includes`/`code_block_decorations`/`attribution_data`/`format_options`/`document_profile`); fixed under item 45 before this item's loop could land. **Option-seed decision**: the aggregation needs `format_crossref_number` for an *unnumbered* chapter too, but P4 had landed the helper with a plain `&ChapterSeed` parameter — widened to the signature design doc §7 actually specifies, `Option<&ChapterSeed>` (`None` → flat `"{order}"`, byte-identical to pre-P4, never "0.1"), with all call sites `Some(...)`-wrapped and a new pin test `unnumbered_chapter_aggregates_flat_never_zero_point_one` in `project_index.rs`; the three remaining `ChapterSeed`-carrying constructions (`crossref_render.rs` ×2, `cross_chapter_crossref_resolve.rs`'s helper) were wrapped in the same change. **Subset-render regression, found by this phase's own gate run**: the new dispatch ignored `RenderMode`, so `q2 render ch1.qmd` on a book project rendered the whole book — `book_project_type::single_chapter_subset_render_keeps_one_file_render_set` failed. Fixed by gating the Html branch on `RenderMode::Full` (Subset falls through to `run_inner()`'s per-document path, the pre-P4 behavior the test pins). The same gate was tried for Typst/Epub and **reverted**: it exposed a pre-existing latent failure — the per-document typst path runs the vendored orange-book template, whose cover code errors on an array `author` (`lib.typ:584`, "expected content, found array") — a path never exercised before P5, so Typst/Epub keep P2's committed merge-always dispatch; fixing the vendored-template invocation is a separate work item. **Stale P4 assertion**: the first P4 test's negative guard `!ch2.contains("Figure\u{a0}1")` false-positived on the now-legitimate resolved cross-chapter link (`"Figure\u{a0}1"` is a prefix of `"Figure\u{a0}1.1"`); narrowed to the exact flat-counter caption string (`Figure\u{a0}1.1: A figure in chapter two`), which cannot legitimately appear, and the file-header comment updated to the P5 world. **E2E finding for P7/Gordon**: the real-CLI render (`cargo run --bin q2 -- render .tmp-p4-probe --to html`, 4 files, all numbers/links correct) still shows one *stale local-pass warning* — ch2's own Crossref phase emits `unresolved crossref @fig-one` before the registry exists, even though the final HTML resolves it. The plan never promised suppressing it (the local diagnostic fires before aggregation is possible), but it is now user-visible noise for every cross-chapter ref; Q1 emits no such warning. Left as-is (a suppression design — "how does the local pass know a registry is coming?" — is not a bug fix), recorded here for P7's docs page or a follow-up.)
- [x] New `CrossChapterCrossrefResolveTransform` (`Finalization` phase, before the existing `CrossrefRenderTransform`, registered unconditionally in the shared `build_transform_pipeline`): reads `ctx.cross_chapter_crossref_registry`, no-ops when `None`; otherwise finds unresolved `CrossrefResolvedRef` nodes, looks them up in the registry, resolves them in-AST — modeled directly on `link_rewrite.rs`'s existing pattern for cross-document link resolution. (`crates/quarto-core/src/transforms/cross_chapter_crossref_resolve.rs`. The walk is `ast_walk::for_each_inline_mut` — the canonical walker `equation_number`/`math_ml` already use — not a hand-rolled one; it covers tables, captions, custom slots, and `Inline::Note` content. Patched keys: `resolved`/`resolved_number`/`target_href`/`order`/`in_appendix`; `target_href` is `page_url_for(owning_chapter_href) + "#" + id` via `ctx.resource_resolver` when present, raw href otherwise. Five unit tests: `cross_chapter_ref_is_patched_from_the_registry` and `sec_ref_gets_owning_chapter_appendix_flag` **observed RED** against the stub; `unknown_id_is_left_unresolved`, `already_resolved_local_node_is_untouched`, `no_registry_leaves_the_ast_untouched` are **guards that pass against the stub by construction** (a no-op leaves everything untouched) and must stay green through any later change. All 5 green after implementation. **Display-side gap closed 2026-09-25**: `render_resolved_ref` now prefers the patched `resolved_number` for non-sec refs (bypassing `format_crossref_number`, which would apply the *rendering* chapter's seed) and `target_href` for the link target; `sec` refs needed no number-side change (`sec_ref_text` re-derives from the patched `order`/`in_appendix`), only the target. Two display tests in `crossref_render.rs` observed RED first (`cross_chapter_ref_renders_owning_number_and_target` — "Figure\u{a0}3.1"/dangling anchor against the stub pairing; `cross_chapter_sec_ref_renders_owning_appendix_shape` — dangling `#sec-methods` target), both green after the change, with the full module's 45 tests green. `pipeline.rs` registration: pushed between `AppendixStructureTransform` and `CrossrefRenderTransform` with the totality rationale in a comment; classified **`Bucket::B3`** in `BUCKETS` (shared post-core service, sibling of `link-rewrite` — pure `plain_data` mutation, no-op without the registry, must survive the Pandoc cut so a future Pandoc-side book writer sees patched numbers); the `build_transform_pipeline` doc's Finalization enumeration renumbered.)
- [x] **Pipeline invariant tests updated — plan correction: there were six, not four.** The handoff said "four pipeline invariant tests (pipeline.rs :5473, :5804, :5867, :5914)"; the actual set that failed when the transform was registered was six: `t1_4_html_render_profile_produces_todays_exact_name_list`, `t1_5_revealjs_render_profile_produces_todays_exact_name_list`, `t2_3_pandoc_docx_profile_produces_exact_surviving_name_list`, `t2_5_html_preview_profile_produces_todays_preview_exact_name_list`, `bucket_classification_is_total_over_the_html_pipeline`, and `neutral_core_invariant_no_b2_b4_survives_the_pandoc_cut`. The last two are derived from `BUCKETS` (fixed by the classification itself); the four exact-name-list tests each gained `"cross-chapter-crossref-resolve"` after `"appendix-structure"` (before `"crossref-render"` on the html/revealjs lists, before `"example-embed-render"` on the docx/preview lists, whose profiles cut `crossref-render`). All six green.
- [x] New `RenderContext` field `cross_chapter_crossref_registry: Option<Arc<ProjectCrossrefIndex>>` + `with_cross_chapter_crossref_registry(...)` builder, matching the `chapter_seed`/`project_index` idiom. (`render.rs`, field + builder + `new()` init.)
- [x] `cargo clippy -p quarto-core --all-targets -- -D warnings`; `cargo nextest run -p quarto-core`; phase-boundary `cargo nextest run --workspace` with delta reported. (All green 2026-09-25. Clippy `-D warnings` clean (two findings fixed: unused `mut` in `multi_file_html.rs`; `.map(..).unwrap_or(false)` → `is_some_and` in a `pipeline.rs` attribution round-trip test). `cargo nextest run -p quarto-core`: **5060 passed / 31 skipped**, including the item-38 TDD driver `cross_chapter_forward_reference_resolves_with_number_and_link` now GREEN and the regression pin `single_chapter_subset_render_keeps_one_file_render_set`. Phase-boundary `cargo nextest run --workspace`: **14913 run / 14913 passed / 200 skipped**, delta vs baseline 14892/200 @ `4d5469eb5` = **+21 passed, skips unchanged** — no stray or duplicated tests. The +21, accounted by file: `book_multifile_html.rs` +5 (9→14, the P5 integration tests written pre-handoff), `project_index.rs` +5 (new file), `cross_chapter_crossref_resolve.rs` +5 (new file), `pipeline.rs` +3 (bridge/round-trip), `crossref_render.rs` +2 (display), `render_to_file.rs` +1 (`resume_finalize_writes_real_html_and_drains_sidebands`); every other touched file ±0 (verified by diffing test-fn attributes against `4d5469eb5`). Workspace run 1 was cancelled at a confirmed **quarto-p2p flake** (`tunnel::client_redials_after_connection_loss`, UDP re-bind timeout at `tunnel.rs:292`, "still failing after 10s") which cancelled ~1117 queued tests; p2p is untouched by P5 and the test passes on an isolated re-run (1 passed / 14 skipped). Full-suite re-run: green as reported. Two phase gates remain outside this checklist: full `cargo xtask verify` (no skips) and `cargo xtask lint`.)

## Details

This phase is where "fidelity to Q1" is weakest by necessity — Q1's DOM-postprocessing approach is architecturally prohibited here, so the *mechanism* is genuinely new even though the *observable result* (a correct cross-chapter reference) must match. Keep the observable-behavior fixtures (the multi-file HTML equivalent of `orange-book`'s cross-chapter assertions, e.g. "Dinosaur 2.1 referenced from chapter 3 shows 2.1, not renumbered") as the actual acceptance bar, not a structural resemblance to Q1's implementation.
