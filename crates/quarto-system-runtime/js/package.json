{
  "name": "quarto-system-runtime-js",
  "version": "0.1.0",
  "private": true,
  "description": "JavaScript bundles for quarto-system-runtime",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:simple": "node esbuild.config.mjs --simple-only",
    "build:ejs": "node esbuild.config.mjs --ejs-only"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "ejs": "^3.1.10"
  }
}
