---
author: Jane Doe
date: "2026-05-10"
format: q2-preview
_quarto:
  tests:
    run:
      requires_js: true
    q2-preview:
      ensureHtmlElements:
        -
          - 'header#title-block-header'
          - 'div.quarto-title-meta'
          - 'div.quarto-title-meta p.date'
        -
          - 'h1.title'
---

Some content. With no `title` but author/date present, the title
block still renders its metadata grid (Q1 parity, bd-gx9cic8z P1 —
the header is gated on any title-block content, not on `title`).
