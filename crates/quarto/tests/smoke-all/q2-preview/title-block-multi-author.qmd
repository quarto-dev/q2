---
title: Doc
author:
  - Alice
  - Bob
date: "2026-05-10"
format: q2-preview
_quarto:
  tests:
    run:
      requires_js: true
    q2-preview:
      ensureHtmlElements:
        -
          - 'div.quarto-title-meta-contents > p'
        -
          - 'div.quarto-title-meta-author'
---

A paragraph with two authors in YAML list form. Each author renders
as its own `<p>` inside `quarto-title-meta-contents`, with the
pluralized "Authors" heading (Q1 parity, bd-gx9cic8z P1 — the old
empty-string-join quirk is gone on both sides).
