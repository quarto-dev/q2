---
title: Doc
date: "2026-05-10"
format: q2-preview
_quarto:
  tests:
    run:
      requires_js: true
    q2-preview:
      ensureHtmlElements:
        -
          - 'header#title-block-header'
          - 'div.quarto-title-meta'
          - 'div.quarto-title-meta p.date'
        -
          - 'div.quarto-title-meta-date'
---

A paragraph; with `date` but no `author`, the Published cell renders
on its own (Q1 parity, title-block epic bd-gx9cic8z P1 — the old
Rust-template quirk of nesting `$if(date)$` inside `$if(author)$` is
gone on both sides).
