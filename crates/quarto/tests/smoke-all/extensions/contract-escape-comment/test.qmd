---
title: Escaped shortcode comment form
format: html
_quarto:
  tests:
    run:
      skip: "Known gap: Q1's {{</* */>}} comment-escape form is not in the qmd grammar (parse error). Deferred to bd-kmo1pzc2 (Hugo interop question); unskip if that strand adds the escape form."
    html:
      noErrors: true
      ensureFileRegexMatches:
        - ["othername"]
---

Literal: {{</* othername */>}}
