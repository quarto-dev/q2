//! Integration-test aggregator (see .claude/rules/integration-tests.md).
//! One binary for all of quarto-hub's integration tests; keep the
//! module list alphabetized.

pub mod admin_scan_real_store;
