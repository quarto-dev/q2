================================================================================
lt-as-str — bare `<` followed by space is a Str (bd-j9cf, `1 < 2`)
================================================================================
1 < 2
--------------------------------------------------------------------------------
    (document
      (section
        (pandoc_paragraph
          (pandoc_str)
          (pandoc_str)
          (pandoc_space)
          (pandoc_str))))
================================================================================
lt-as-str — bare `<` at end of line is a Str
================================================================================
foo <
--------------------------------------------------------------------------------
    (document
      (section
        (pandoc_paragraph
          (pandoc_str)
          (pandoc_str))))
================================================================================
lt-as-str — bare `<` followed by digit is a Str
================================================================================
a <5 b
--------------------------------------------------------------------------------
    (document
      (section
        (pandoc_paragraph
          (pandoc_str)
          (pandoc_str)
          (pandoc_str)
          (pandoc_space)
          (pandoc_str))))
================================================================================
lt-as-str — `<b>` still parses as html_element (regression)
================================================================================
<b>
--------------------------------------------------------------------------------
    (document
      (section
        (pandoc_paragraph
          (html_element))))
================================================================================
lt-as-str — autolink still parses as autolink (regression)
================================================================================
<https://example.com>
--------------------------------------------------------------------------------
    (document
      (section
        (pandoc_paragraph
          (autolink))))
================================================================================
lt-as-str — HTML comment still parses as comment (regression)
================================================================================
<!-- c -->
--------------------------------------------------------------------------------
    (document
      (section
        (pandoc_paragraph
          (comment))))
================================================================================
lt-as-str — `<foo` with no closing `>` parses as Str `<` + Str `foo`
================================================================================
a <foo
--------------------------------------------------------------------------------
    (document
      (section
        (pandoc_paragraph
          (pandoc_str)
          (pandoc_str)
          (pandoc_str))))
