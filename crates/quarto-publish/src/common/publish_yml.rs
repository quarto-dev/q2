//! `_publish.yml` reader (Q1-compatible). **Phase 0 stub** — read
//! support lands in Phase 1. Q1's writer is *not* required for the
//! gh-pages provider (the branch itself is the deployment record).
