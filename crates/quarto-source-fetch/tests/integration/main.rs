//! quarto-source-fetch integration test binary.
//! See `.claude/rules/integration-tests.md` for why tests live here
//! rather than as separate top-level `tests/*.rs` files.

pub mod extract;
pub mod network;

fn main() {}
